# 측정 — 최대 성능 재측정 (1 GbE 링크, 2026-09-05)

- **리포**: `dev` = `95740c5`, 버전 `1.3.0`, 배포 바이너리 `v1.0.9-27-g6df0c8b-dirty`
- **선행**: [온링크 전환 후 계단 (2026-08-06)](../2026-08-06-postmove-loadtest/REPORT.md) — 같은 계단을 **100 Mbps 링크**에서 · [c 복구 후 재검증 (08-31~09-02)](../2026-08-31-postfix-recheck/REPORT.md) — 지속 18 통과
- **동기**: `c` 의 링크가 08-31 현장 조치로 **100 → 1000 Mbps** 가 됐다. 08-06 계단의 붕괴점(각 34 Mbps)에서 c 회선이 약 180 Mbps(RX+TX) = **당시 링크 방향당 90%** 였으므로, 그 붕괴가 CPU 가 아니라 링크 포화였을 가능성이 있었다.

---

## 0. 결론

| | 값 | 근거 |
|---|---|---|
| **지속 상한 (1시간 검증)** | **앱 각 방향 20 Mbps** | 2회 완주. 재보정 기본 임계에서 **c→s 순손실 0**/7,211,537 · s→c 0.0025%, 누수 0 (§2-다·§3-라) |
| 지속 한계 | **20 < X < 24** — 22 는 손실 없이 38분을 가나 코어 90% 상주로 **권고 불가** | §2-나·§2-가 |
| 버스트 한계 (45초) | 각 22~24 Mbps | §1 |
| **병목** | **`c` 의 단일 코어** — 링크가 아니다 | 링크 10배 증설에도 한계 불변 (§1) |
| 회선 증폭 (R=20) | **2.69배** (앱 40 → 회선 107.5 Mbps) | 사이징 식 예측과 일치 |

**08-06 의 "34 Mbps 붕괴" 는 링크 바운드였다** — 1 GbE 에서 같은 레이트가 실효를 다 내고(33.8M) 손실이 14% → 0.59% 로 24배 낮다. 그러나 **실용 한계는 22~24 로 그대로**다. 링크를 10배로 키워도 안 움직였으므로 제약은 `c` 의 CPU(단일 스레드 + 수신 softirq)로 확정된다.

---

## 1. 계단 (`mf_ladder_udp.sh`, 단계당 45초)

| 각 방향 | c→s 실효 | s→c 실효 | c→s 손실 | s→c 손실 | c 전체 CPU | c 최고1코어 | OoO | 판정 |
|---|---|---|---|---|---|---|---|---|
| 18 | 18.0M | 18.0M | **0%** | 0.0086% | 36.7% | 69.1% | 3,820 | ✅ |
| 22 | 22.0M | 22.0M | 0.014% | 0.029% | 50.8% | 72.8% | 4,726 | ✅ |
| 24 | 24.0M | 24.0M | **0%** | 0.05% | 62.5% | 82.6% | 5,068 | ✅ 경계 |
| 26 | 26.0M | 26.0M | 0.0017% | 0.061% | 57.5% | 79.4% | 5,612 | ⚠️ 런에 따라 트립 |
| 30 | — | — | — | — | — | **94.0%** | — | ❌ 가드 트립 |
| 34 | 33.8M | 33.8M | 0.59% | 0.58% | 58.7% | 75.5% | 9,839 | ⚠️ 손실 (가드 부재 — §4-가) |

**26 은 경계다.** 첫 런은 1코어 `86.9 / 85.7 / 86.6%` 3연속 초과로 트립, 두 번째 런은 79.4% 로 통과. 문서화된 1초 창 편차 ±5~9p 안이다.

> ⚠️ **계단은 CPU 를 과소평가한다.** 단계 시작 8초 뒤 3회 평균이라 램프 구간의 낮은 값이 섞인다. 같은 22 Mbps 가 계단에서 72.8%, 지속 소크의 연속 샘플에서는 **85~90%** 로 읽혔다(§2-나). 계단 CPU 값을 임계 산정 근거로 쓰면 안 된다.

---

## 2. 지속 소크 (`mf_soak_18m.sh`, 운영 체인 :443)

### 2-가. 각 24 Mbps — 2분 57초에 붕괴 ❌

가드 트립(1코어 90.6% · 전체 72.0%)이 **손실 폭발 직후**에 왔다. 클라 집계는 SIGKILL 로 유실됐고 `s` 수신 로그로 초 단위 복원했다.

| 구간 | 실효 | c→s 손실 |
|---|---|---|
| 0–30초 | 23.94 | 0.048% |
| 30–150초 | 24.00 | 0.008 ~ 0.021% |
| **150–180초** | 23.75 | **0.948%** |

**150초까지 완벽했다.** 169초에 351개(그 초의 **15.1%**), 170초에 148개(5.8%)가 한꺼번에 사라졌다 — 전체 136초 중 손실이 난 35초 가운데 499개가 이 2초에 몰렸다. 회선은 1 GbE 의 12.9% 뿐이라 링크와 무관하다.

**45초 계단에서 24 가 통과한 이유는 창이 이 사건 전에 끝나기 때문이다.**

### 2-나. 각 22 Mbps — 지속 가능하나 여유가 없다 ⚠️

**두 단계로 측정했다.** 구 임계(85)에서는 3회 모두 19~24초에 트립했고 손실은 0이었다 — 코어가 85 선에 상주하니 즉시 걸린 것이다. 재보정된 기본 임계(68 / 90)로 다시 돌리자 **38분 24초**를 갔고, 손실·누수·드리프트가 전혀 없는 채 1코어 90.2% 3연속으로 트립했다.

| | 값 (재보정 임계, 38분 24초) |
|---|---|
| c→s 손실 | **635 / 5,067,977 = 0.0125%** |
| 실효 | **22.00 Mbps** 고정 |
| 5분 구간별 손실 | 0.0129 · 0.0135 · 0.0145 · 0.0107 · 0.0141 · 0.0107 · 0.0110 · 0.0129% — **완전 평탄** |
| RSS | c 15.01 · r 1.36 · s 37.14 MB — **세 호스트 모두 변화 0회** |
| FD | 8 / 14 / 8 고정 |
| c 프로세스 CPU | p50 **83.4%** / max 84.8 (6구간 79.4 → 83.3 → 83.7 → 83.7 → 82.9 → **82.6**) |
| c 머신 전체 | p50 **46.1%** / max 48.0 — 임계 68 대비 20p 여유 |
| 가드 | 초과 **72건 · 트립 1건** — 그중 **70건이 `c` 1코어**, 값 분포 **90.1 ~ 91.8 (p50 90.2)** |

**손실도 CPU 도 시간에 따라 나빠지지 않는다.** 24 Mbps 와는 성질이 완전히 다르다 — 24 는 169초에 한 초의 15.1% 가 사라졌다.

**그런데 코어가 임계 위에 상주한다.** 20 Mbps 의 1코어 대역은 85.1~87.5 로 90 아래에 확실히 들어갔는데, 22 는 **90.1~91.8** 로 임계를 걸치고 앉는다. `multi-fec` 은 단일 스레드이므로 **1코어 100% 가 절대 상한**이고, 프로세스 자체가 이미 코어의 **83.4%** 를 쓴다 — 남은 여유가 17p 뿐이다.

> **판정: 22 Mbps 는 지속 상한으로 권고하지 않는다.** 손실이 없으니 "동작한다" 고 말할 수는 있으나, 단일 스레드의 절대 상한에 90% 로 상주하는 상태다. 순간 부하(재전송 버스트, 배경 작업)가 얹히면 24 에서 본 손실 폭발까지 남은 거리가 없다.
>
> ⚠️ **이 트립을 없애려고 임계를 또 올리면 안 된다.** 90 → 95 로 올리면 22 는 완주하겠지만, 그것은 **실제 병목(단일 코어)에 남은 마지막 가드레일을 치우는 것**이다. 20 의 오탐 해소(85 → 90)는 정상 대역이 임계 아래에 명확히 있었기 때문에 정당했지만, 22 는 대역 자체가 임계 위에 있다 — 계측 문제가 아니라 **부하가 실제로 한계에 가까운 것**이다.

### 2-다. 각 20 Mbps — 1시간 완주 ✅ (새 지속 상한)

두 번 돌렸다. 첫 런(기본 임계)은 **32분 무열화 후** 1코어 87.5% 로 트립했고, `GW_CORE=90` 으로 다시 돌려 완주했다(§3-나).

| | 값 |
|---|---|
| c→s | **109 / 7,211,538 = 0.0015%** |
| s→c | **136 / 7,211,540 = 0.0019%** |
| 실효 | 양방향 **20.0 Mbps** — 60개 구간 전부 |
| 회선 | 107.5 Mbps (RX 53.7 + TX 53.8) = **증폭 2.69배** |

**손실이 시간에 따라 커지지 않는다** (10분 단위, s→c):

```
0.0067% → 0.0008% → 0.0003% → 0.0033% → 0.00008% → 0.00008%
                                          ↑ 마지막 20분은 10분에 1패킷
```

**자원 — 누수 0, 드리프트 없음**

| | RSS | 변화 | FD | CPU 6구간 |
|---|---|---|---|---|
| c 클라이언트 | 15.01 → **15.01 MB** | **0회** | 8 고정 | 75.6 → 78.8 → 79.9 → 79.3 → 77.9 → **78.4** (Δ+2.8p) |
| r 릴레이×2 | 1.36 → 1.36 MB | 0회 | 14 고정 | 51.0 → … → 52.0 (Δ+1.0p) |
| s 서버 | 37.14 → 37.14 MB | 0회 | 8 고정 | 32.4 → … → 32.9 (Δ+0.5p) |

**RSS 가 1시간 동안 1 KB 도 변하지 않았다**(세 호스트 전부). CPU 는 1구간 워밍업 뒤 평탄하고 후반에 내려간다.

첫 런(임계 85, 32분)도 같은 성질이었다 — 손실 5분 구간 0.0115~0.0145% **완전 평탄**, CPU 73→80→77.9% (하락), RSS 15.01 MB 변화 0회.

---

## 3. 가드 계측 — 도구가 피측정계를 흔든다

### 3-가. ssh 오버헤드와 ControlMaster (적용 완료)

가드는 샘플마다 원격에 ssh 를 새로 맺는다. Atom N2600 에서는 sshd + 키교환이 코어를 크게 흔든다. **트래픽 0 인 상태로 A/B** (인스턴스 수를 `ps` 로 검증하고 측정):

| c 상태 | 전체 busy | cpu0 | cpu1 | cpu2 | cpu3 |
|---|---|---|---|---|---|
| 가드 없음 | **0.5~0.7%** | 0.2 | 0.2 | 0.5 | 1.1 |
| 가드 (수정 전) | **11.5%** | 17.0 | 9.4 | 11.2 | 8.9 |
| **가드 (ControlMaster)** | **4.2%** | 4.5 | 3.6 | 4.7 | 4.3 |

**수정**: `mf_gwguard.sh` 의 ssh 호출 5곳을 래퍼로 교체. **임계값·판정 로직 무변경.**

```bash
SSH() { command ssh -o ConnectTimeout=5 \
        -o ControlMaster=auto -o "ControlPath=$GW_CM_DIR/%r@%h:%p" -o ControlPersist=120 "$@"; }
```

백업은 로컬 `mf_gwguard.sh.bak-precontrolmaster` 와 git 이력. 오버헤드 **11.5% → 4.2%**(코어당 8~17p → 3.6~4.7p).

### 3-나. 1코어 임계 85% 는 이 레이트에서 오탐한다

20 Mbps 첫 런은 **32분간 손실·CPU·메모리 어느 것도 나빠지지 않았는데** 1코어 87.5% 로 트립했다. 그 32분 동안 단발 초과가 **107건**이었고 연속 3회가 우연히 겹친 순간 잘렸다.

`GW_CORE=90` 으로 올리자 **초과가 8건으로 줄고 1시간을 완주**했다. 그 8건도 대부분 `c` 가 아니라 `r` 이었다:

```
15:23  r 1코어 94.9 / 95.0 / 97.0%      ← r 프로세스 CPU 는 52% 평탄. VM 호스트 부하 또는 샘플 창
15:53  r 1코어 100.0 / 99.0%
15:32  c 전체 62.4% (임계 62)            ← 단 1회, 경계 스침
16:10  c 1코어 93.2%                     ← 종료 직전
```

**85 의 근거가 흔들렸다.** 그 값은 08-06 계단(3회 평균)으로 정했는데, 위 §1 경고대로 계단은 CPU 를 과소평가한다. 정상 구간인 c 1코어 78~80% 에 임계가 붙어 있어 오탐한다.

> **재보정은 하지 않았다** — 90 은 08-06 에 34 Mbps 붕괴(1코어 85.7%)를 놓친 값이다. 이번 런은 `GW_CORE=90` 을 **그 런에 한해** 넘겨 돌렸고, 백스톱으로 c 전체 62%·손실 초 단위 관측·실효 이탈을 두었다. 항구적 임계 변경은 별도 판단이 필요하다.

### 3-다. 임계 재보정 (2026-09-05, 적용 완료)

이전 값(전체 62 / 1코어 85)은 **45초 계단**에서 정했다. 계단은 CPU 를 과소평가하므로 지속 부하에서는 정상 구간에 임계가 붙는다. **지속 소크의 1초 샘플**로 다시 정했다.

| 런 | c 전체 (초과 샘플 값) | c 최고1코어 |
|---|---|---|
| 정상 16M 1시간 | 62.3 · 62.5 | 초과 없음 |
| 정상 18M 1시간 | 63.5 ~ 64.3 (4회) | 초과 없음 |
| 정상 20M 32분 | 초과 없음 | **85.1 ~ 87.5 (106회, 88 이상 0회)** |
| 정상 20M 1시간 | 62.4 (1회) | 93.2 (1회, **종료 직전**) |
| **붕괴 24M** (169초에 손실 15%) | **72.0 · 72.0 · 76.1** | 90.6 · 92.5 · 94.2 |
| **실패 18M** (08-07, 21분) | **62.4 → 73.5 (31회, 상승 추세)** | 91.1 |

**`c` 전체가 진짜 판별자다** — 정상은 최대 **64.3**, 붕괴·실패는 **72 이상**으로 확실히 갈린다. 최고1코어는 정상 대역(≤87.5)과 붕괴(90.6~94.2)가 갈리기는 하나 종료 전환에서 93.2 가 한 번 나와 여유가 좁다.

```
c 전체   62 → 68   1차 판별자. 정상 최대 64.3 대비 +3.7p, 붕괴 72.0 대비 −4.0p
1코어    85 → 90   보조. 정상 대역 87.5 대비 +2.5p. 20 Mbps 1시간 완주로 검증
r·s 전체 75, 링크 400 Mbps — 무변경
```

**기록된 로그 재생 검증** (연속 3회 = 트립):

| 런 | 전체 >68 | 1코어 >90 | 구 임계 → 신 임계 |
|---|---|---|---|
| 정상 16M / 18M 1시간 | 0 | 2 / 0 | 통과 → **통과** |
| 정상 20M 32분 | 0 | 0 | **트립 → 통과** ← 오탐 해소 |
| 정상 20M 1시간 | 0 | 1 | 통과 → **통과** |
| **붕괴 24M** | **3** | **4** | 트립 → **트립** |
| **실패 18M (08-07)** | **7** | **4** | 트립 → **트립** |

> ⚠️ **이 가드는 붕괴 탐지기가 아니다.** 24M 붕괴는 60초 간격 샘플에서 최고1코어가 78~80% 로 **정상 20M(86.9%)보다 낮게** 보였다 — CPU 로는 예고되지 않고 손실이 먼저 터진다. 손실 SLO 는 수신측 초 단위 계측으로 봐야 하고 가드로 대신할 수 없다. 가드의 역할은 **호스트 과부하와 증폭 폭주를 멈추는 백스톱**이다.

---

### 3-라. 재보정 실측 검증 (2026-09-06)

재보정된 **기본 임계(68 / 90)** 로, 환경변수 없이 20 Mbps 1시간을 다시 돌렸다.

| | 값 |
|---|---|
| c→s | **0 / 7,211,537 = 0%** (순손실 0) |
| s→c | 182 / 7,211,537 = 0.0025% |
| 실효 | 양방향 20.0 Mbps 고정 |
| **가드** | **트립 0 · 초과 4건** |
| c 머신 전체 | p50 **51.3%** / max **53.9%** — 임계 68 대비 14p 여유 |
| RSS | c 15.01 · r 1.36 · s 37.14 MB — **세 호스트 모두 변화 0회** |
| FD | 8 / 14 / 8 고정 |

초과 4건 중 **`c` 관련은 종료 6초 전 1건뿐**이고 나머지는 전부 `r` 이다.

```
12:53:26  r 1코어 93.1%   ┐
12:53:27  r 1코어 96.0%   ├ 릴레이 프로세스 CPU 는 51~52% 로 평탄 (§5-5 미규명)
13:23:24  r 1코어 99.0%   ┘
13:26:37  c 1코어 100.0%  ← 런 종료 6초 전, 정리 전환 구간
```

**`c` 전체 CPU 는 한 번도 넘지 않았다** — 1차 판별자로 삼은 근거가 실측으로 확인됐다.

| 런 | 임계 | 초과 | 트립 | 결과 |
|---|---|---|---|---|
| 20M 32분 | 62 / 85 | **106** | **1** | 32분에 잘림 (오탐) |
| 20M 1시간 | 62 / 90 (override) | 8 | 0 | 완주 |
| **20M 1시간** | **68 / 90 (기본값)** | **4** | **0** | **완주, c→s 순손실 0** |

손실은 10분 단위로 0.0064 → 0.0039 → 0.0017 → 0.0003 → 0.0041 → 0.0003% 로 **시간이 갈수록 줄어든다.** CPU 도 1구간 워밍업 뒤 평탄하다(76.2 → 79.8, Δ+3.6p).

---

## 4. 절차 실패 기록 (측정자 과실)

### 4-가. 34 단계가 가드 없이 돌았다

30 에서 가드가 트립해 종료했는데 하네스의 생존 검사 `pgrep -f '[m]f_gwguard.sh watch'` 가 **종료 중이던 프로세스를 매치**해 34 를 그대로 진행했다. 피해는 없었다(45초 만에 종료, c 58.7%, 회선 1 GbE 의 18%, 잔여 부하 0). **조치 필요**: 생존 검사를 **가드 PID 보관 후 `kill -0`** 으로 바꿀 것. 측정 중 하네스를 바꾸면 앞뒤 단계가 비교 불가라 이번 런에서는 고치지 않았다.

### 4-나. `pkill -f` 가 자기 셸을 죽여 가드가 누적됐다 — 하루에 3회

`pkill -f "mf_gwguard.sh watch"` 는 **패턴이 자기 명령줄에 평문으로 들어가** 호출 셸을 먼저 죽인다(exit 144). 그 결과 가드가 살아남아 누적됐고, 22 Mbps 트립 2건에 오염이 섞여 재현 확인에 런 2회를 더 썼다. **리포에 이미 기록된 함정**(하네스 함정 #4 계열)인데 오늘 세 번 반복했다.

**교훈**: 정리는 `ps -eo pid,args` 로 목록을 만든 뒤 **PID 로 kill** 한다. 문자열 매치를 쓰지 않는다.

### 4-다. 인스턴스 개수를 mux 소켓까지 세었다

`ps | grep mf_gwguard` 가 ControlMaster mux(`ssh: /tmp/mf_gwguard_cm/...`)까지 매치해 "가드 5개" 로 보였다. 실제 가드 루프는 그보다 적었다. 세는 패턴은 `mf_gwguard(\.sh)? (watch|sample)` 로 좁혀야 한다.

---

## 5. 남은 것

| 순위 | 항목 | 비고 |
|---|---|---|
| ~~1~~ | ~~가드 임계 재보정~~ | ✅ **완료 (§3-다)** — 전체 62 → 68, 1코어 85 → 90. 정상 4런 통과 · 붕괴 2런 트립으로 재생 검증 |
| 2 | `mf_ladder_udp.sh` 생존 검사 → PID `kill -0` | §4-가 |
| ~~3~~ | ~~22 Mbps 의 지속 가부~~ | ✅ **완료 (§2-나)** — 38분 24초 무열화 후 트립. 손실 없으나 1코어 90.1~91.8% 상주로 **상한 권고 불가**. 지속 상한은 20 유지 |
| ~~4~~ | ~~재보정된 기본 임계로 20 Mbps 완주 재확인~~ | ✅ **완료 (§3-라)** — 트립 0 · 초과 4건(c 는 종료 직전 1건) · c→s 순손실 0 |
| 5 | r 의 1코어 93~99% 스파이크 | 프로세스 CPU 는 51~52% 평탄. 재보정 후에도 남은 유일한 초과원(3/4건). VM 호스트 부하 의심, 미규명 |

## 부록 — 산출물

```
raw/            계단 3런 합본 + iperf3 원본 6개 + 가드 로그 2개
soak24/         24 Mbps — srv.log(유일한 전달 기록) · watchdog.log · sample_{c,r,s}.csv
soak22/ 22b/ 22c/  22 Mbps 3회 — 전부 트립, 손실 0
soak20/         20 Mbps 기본 임계 — 32분 무열화 후 트립
soak20b/        20 Mbps GW_CORE=90 — **1시간 완주**. iperf.log 에 클라 최종 집계 있음
```

측정 후 테스트망 원복 확인: `no-autoheal` 제거, 전 유닛 active, 잔여 부하·가드 0, WG RTT 55.4 ms(기준 54.1).
