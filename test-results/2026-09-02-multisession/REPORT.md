# 측정 — 세션별 WireGuard 다중 세션 소크 (2026-09-02)

- **리포**: `dev` = `ec9cdc2`, 버전 `1.3.0`, 배포 바이너리 `v1.0.9-27-g6df0c8b-dirty`
- **원본**: `test-results/2026-09-02-multisession/`
- **관련**: [지속 한계 재검증 (08-31~09-02)](../2026-08-31-postfix-recheck/REPORT.md) — 단일 세션 12/14/16/18

---

## 0. 요약

**실서비스 목표 구조(지사 10곳 × 평균 1 Mbps)를 세션별 WireGuard까지 포함해 1시간 재현했고,
전 항목을 통과했다.**

| 항목 | 결과 |
|---|---|
| 왕복 전달 | **3,749,983 / 3,750,000 = 99.9995%** (유실 17) |
| **오배송** | **0** — 10세션 전부 남의 태그 수신 0건 |
| 완주 | ✅ 3600초 · 가드 트립 0 · **무감시 구간 0** |
| 누수 | **없음** — c·s 마지막 15분 기울기 **+0.00 MB/h**, FD 고정 |
| 생성기 건전성 | `pacing_resets=0` · `sendfail=0` — 레이트 정확히 유지 |

### ⚠️ 사이징 식이 회선 소요를 36% 과소평가한다

| | 값 |
|---|---|
| 앱 페이로드 | 각 방향 10 Mbps (10세션 × 1) |
| WG 터널 통과량 | **21.03 Mbps** (양방향 합, WG/UDP 헤더 포함) |
| **실제 회선** (c `enp2s0`) | **72.76 Mbps** |
| **증폭** | **3.46배** (WG 터널 기준) / 3.64배 (순 페이로드 기준) |
| 기존 사이징 식 예측 | `5.34 × R × N` = **53.4 Mbps** |
| **오차** | **실측이 1.36배 크다** |

사이징 식의 `2.67배`는 **고레이트 측정에서 나온 값**이다. 지사당 1 Mbps 는 FEC 그룹 크기
`g` 가 1~2 에 머무는 구간이라 패킷당 패리티 비율이 가장 나쁘다 — 저레이트 오버헤드 경고가
실제로 확인된 것이다. **10지사 배포의 회선 산정은 3.5배로 다시 해야 한다.**

---

## 1. 왜 다시 쟀나 — 앞선 다중 세션 시험은 WG 가 없었다

같은 날 오전에 돈 `mf_multi_soak.sh`(4세션 × 4 Mbps)에는 **WireGuard 가 하나도 없었다.**

```
(오전, 잘못된 구조)  blaster → multi-fec client → relay×2 → server → rt_echo
```

하네스에 보이는 `--wg 127.0.0.1:44444` 는 **multi-fec 의 옵션 이름**일 뿐 실제 WG 가 아니라
Python echo 를 가리킨다. 그런데 실서비스는 **지사마다 독립 WG 터널**이라 세션 수만큼 암복호가
붙는다. 그 비용이 빠진 측정은 낙관적으로 치우친다.

> **사용자 지적으로 발견했다.** 그 전까지 문서에는 "병렬 체인에는 WG 가 없어 단일 세션과 CPU
> 비교 불가"라는 주의만 달려 있었는데, 실제 문제는 비교 가능성이 아니라 **목표 구조를 재현하지
> 못한 것**이었다.

---

## 2. 구조

```
지사 i (i=0..9):
  [mf_blast] 10.9.2{i}.2 ──wg mft{i} (MTU 1300)── 10.9.2{i}.1 [rt_echo_ip]
       c: Endpoint=127.0.0.1:5190{i}                s: ListenPort=5190{i}
            │                                              ▲
       multi-fec client{i} ─┬─▶ r .85:4443 ─┐              │
                            └─▶ r .86:4443 ─┴─▶ multi-fec server{i} (.84:450{i})
                                릴레이 2개 · --route 로 10개 키 분기
```

설정: `duplicate` · `--mode 1` · `-f 5:1,20:4` · `--fec-timeout 10` · `--mtu 1350` ·
`--decode-buf 2000` · `--auth-interval 60` · WG `MTU 1300` · netem 25ms/5% · 30ms/2%

**운영 무침습** — 이름·주소·포트를 완전히 분리했다.

| | 시험 | 운영 |
|---|---|---|
| WG 이름 | `mft0`~`mft9` | `starlink-fec` |
| 서브넷 | 10.9.20~29 | 10.9.9 / 10.9.10 |
| 클라 포트 | 51900~51909 | 51821 |
| 서버 포트 | 4500~4509 | 443 |
| 릴레이 | .85/.86**:4443** | .85/.86**:443** |

종료 후 `mft*` 전부 제거, 운영 체인 `active`, netem 원복 확인.

---

## 3. 결과

### 3-가. 전달·오배송

| 세션 | 송신 | 회수 | 회수율 | 남의 태그 |
|---|---|---|---|---|
| 0 | 375,000 | 374,996 | 99.9989% | **0** |
| 1·2·5·6 | 375,000 | 375,000 | **100.0000%** | **0** |
| 3·4·9 | 375,000 | 374,998 | 99.9995% | **0** |
| 7 | 375,000 | 374,997 | 99.9992% | **0** |
| 8 | 375,000 | 374,996 | 99.9989% | **0** |
| **합계** | **3,750,000** | **3,749,983** | **99.9995%** | **0** |

**오배송 0 이 이 시험의 핵심 성과다.** §19-가(다중 클라이언트 다운스트림 오배송)가
세션별 WG 가 붙은 실서비스 구조에서도 재발하지 않음을 확인했다. 왕복 경로이므로
**하향까지 검증**된다.

### 3-나. CPU — c 가 압력을 받지만 버틴다

| | 값 |
|---|---|
| c 머신 busy (10초 창 평균) | **49.7%**, 최대 55.7%, 드리프트 **+0.7p** |
| 가드 관측 (1초 창) | **초과 30건**, 최대 `c:전체=69.6%`. **3회 연속 없음 → 트립 0** |

두 값이 다른 이유는 **창 길이**다 — 가드는 1초, 샘플러는 10초 평균이라 순간 피크가 가드에만
보인다. 초과 30건은 c 가 임계선 근처에서 흔들렸다는 뜻이고, 실제로 이번이 지금까지
**가드 초과가 가장 많았던 런**이다.

**CPU 분해** (논리 CPU 4개 기준):

```
multi-fec 클라 10개   1.06 논리CPU   (프로세스 CPU 합 105.6%)
부하 생성기            0.18          (mf_blast, 18.0%)
나머지                 0.75          ← WG 암복호 10개 + 수신 softirq + 시스템
──────────────────────────────
머신 총계              1.99 논리CPU  (= 49.7% × 4)
```

**WG + softirq 가 전체의 38% 를 쓴다.** 오전의 WG 없는 시험이 놓친 부분이 이것이다.

> ⚠️ **생성기 0.18 논리CPU 는 실서비스에 없는 비용**이다. 제품만의 몫은 약 1.81 논리CPU 다.
> 반대로 실서비스에서는 지사 10곳이 **각자 다른 호스트**이므로 클라 측 1.06 은 10대에 나뉜다 —
> 이 시험은 c 한 대가 지사 10곳을 흉내낸 **최악 조건**이다.

### 3-다. 자원

| 호스트 | 프로세스 | RSS | 마지막 15분 | FD |
|---|---|---|---|---|
| c | 클라 10 | 111.4 → **130.6 MB** (13.1/개) | **+0.00 MB/h** | 70 (7/개) |
| s | 서버 10 | 111.2 → **130.0 MB** (13.0/개) | **+0.00 MB/h** | 70 |
| r | 릴레이 2 | 3.0 MB | −0.41 MB/h | 32 |

RSS 궤적은 **첫 10분에 램프(+112 MB/h)를 마치고 이후 완전 평탄**하다. 30분 창으로 보면
c +0.51 / s +1.01 MB/h 가 나오는데, 이는 창 안에 들어온 0.2~0.5 MB 계단 하나 때문이고
**15분 창에서는 양쪽 다 정확히 0** 이다. 누수 아님.

> 연결당 13 MB 는 기존 모델(`2.2 MB + decode-buf × 3,828 B` = 9.9 MB)보다 크다.
> 10연결 = 130 MB 이므로 **10지사 서버 호스트 메모리 산정은 100 MB 가 아니라 130 MB** 로 본다.

### 3-라. 회선

| 인터페이스 | RX | TX | 합 |
|---|---|---|---|
| c `enp2s0` | 35.29 | 37.47 | **72.76** |
| r `ens18` | 73.79 | 72.50 | **146.29** |
| s `ens18` | 37.34 | 36.43 | **73.77** |
| c·s `mft0`~`mft9` (각) | 1.05 | 1.05 | 2.10 |

`r ens18` 146.29 ≈ c 72.76 + s 73.77 = 146.53 — **장부가 맞는다**(릴레이가 받아 되보냄).
테스트망 밖 인터페이스는 전부 유휴였다.

---

## 4. 10지사 배포에 주는 함의

| 항목 | 종전 산정 | **이번 실측** |
|---|---|---|
| 회선 (R=1, N=10) | 53.4 Mbps | **72.8 Mbps** (1.36배) |
| 증폭 계수 | 2.67배 | **3.46배** (저레이트 구간) |
| 서버 호스트 메모리 | 약 100 MB | **약 130 MB** |
| 릴레이 부하 | — | 2 프로세스로 146 Mbps 중계, RSS 3 MB, CPU 41% |
| 동시 슬롯 | 20 / 32 | 확인됨 (서버 프로세스별로 2슬롯) |

**peak(지사당 10 Mbps) 는 아직 시험하지 않았다.** 그 구간은 `g` 가 커져 증폭이 2.6배 쪽으로
내려가므로 회선은 3.46배 선형 외삽보다 작을 것으로 보이나, **추정이며 측정이 필요하다.**

---

## 5. 하네스

새로 만든 것 — `mf_wg_multi.sh`(203줄), `mf_blast.c`(C 부하 생성기), `rt_echo_ip.py`.

**`mf_blast` 가 Python 생성기를 대체한 이유는 CPU 가 아니라 격리다.** sv1 A/B 에서 Python 도
레이트는 정확히 냈다(22% vs C 17%). 다만 C 판은 RSS 0.77 MB 로 가볍고, 이번 런에서
**생성기 비용 0.18 논리CPU 를 분리 계측**할 수 있게 해줬다 — 그게 실제 이득이었다.

### 이번에 잡은 하네스 결함 3건

**① 가드가 트립하면 나머지 런이 무감시가 된다** (오전 4×4 런에서 실제 발생).
트립 시각에 대상 프로세스가 아직 없으면 아무것도 못 죽이고 가드가 종료한다 → 그대로 1시간을
무감시로 돌았다. **부하와 병렬로 가드 생존을 감시하고 사라지면 부하를 중단**하도록 고쳤다.
이번 런에는 `guard_died.log` 가 없다.

**② 샘플러 패턴이 운영 프로세스를 함께 센다.** kill 패턴은 `518[6-9]` 로 고쳐져 있었는데
**샘플러 패턴은 `518` 그대로**였다 — 운영 클라(:51821)와 운영 릴레이(.85:443)가 합산돼
누수 판정이 무의미해진다. 양쪽에 포트 범위를 넣었다. 이번 런은 `nproc` 이 정확히 10/10/2 다.

**③ 샘플러 패턴이 실제 cmdline 과 안 맞았다.** `mf_blast --sessions` 로 잡으려 했으나 실제는
`mf_blast --no-clients --sessions …` 라 **사이에 낀 옵션 때문에 매치 실패** → CSV 가 헤더만
남았다. 패턴을 이름만으로 줄였다(래퍼 셸은 `comm` 필터가 걸러낸다).

> 세 건 모두 **조용히 틀린 값을 내는** 종류다. 크래시하지 않고 그럴듯한 결과가 나오므로
> 결과를 먼저 의심하지 않으면 발견되지 않는다.

---

## 6. 남은 것

| 순위 | 항목 | 비고 |
|---|---|---|
| 1 | **peak 프로파일 (지사당 10 Mbps × 10)** | 총 100 Mbps 앱. c 한 대로는 불가 — 세션 수를 줄이거나 부하를 나눠야 한다 |
| 2 | **증폭 곡선** | 저레이트 3.46배 / 고레이트 2.58배. 그 사이를 몇 점 재면 사이징 식을 다시 세울 수 있다 |
| 3 | 24~72시간 소크 | 최장이 여전히 5시간(단일 체인)이다 |
| 4 | 오전 4×4 런 재실행 | WG 없이 잰 값이라 폐기. 필요하면 WG 붙여 다시 |
| 5 | 이중화 (SPOF 2개) | 공유 릴레이 호스트·서버 호스트. 성능이 아니라 가용성 항목 |

---

## 부록 — 산출물

```
test-results/2026-09-02-multisession/
├── REPORT.md            이 문서
├── mf_wg_multi.sh       세션별 WG 다중 세션 하네스
├── mf_blast.c / mf_blast  C 부하 생성기 (정적, 경고 0)
├── rt_echo_ip.py        WG 주소에 바인딩하는 echo
├── rt_sample.sh         샘플러 (comm 인자 일반화)
├── mf_multi_soak.sh     ⚠️ WG 없는 구버전 — 결과 폐기, 참고용
├── wg_smoke/            2분 스모크
└── wg_raw/              본 런 (1시간)
    ├── result.csv       세션별 전달·오배송
    ├── blast.err        pacing_resets 등 하네스 건전성
    ├── sample_{c,r,s}.csv · sample_blast.csv
    ├── ifsnap.txt · watchdog.log
```
