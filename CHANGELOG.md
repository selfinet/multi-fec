# 변경 이력 (CHANGELOG)

multi-fec의 **버전·작성일자별 변경 내용**을 기록한다. 버전별로 무엇이 언제 바뀌었는지 비교하기 위한 문서다.

## 기록 규칙

- 버전은 `common.h`의 `MULTI_FEC_VERSION`(semantic versioning)과 **항상 일치**시킨다.
- 변경 성격에 따라 버전을 올린다:
  | 변경 성격 | 버전 증가 | 예 |
  |-----------|-----------|-----|
  | 와이어 포맷·프로토콜·비호환 변경 | **MAJOR** (`x.0.0`) | obfs 헤더 구조 변경, 세션 포맷 변경 |
  | 기능 추가(하위 호환) | **MINOR** (`0.x.0`) | 새 `--mode`, 새 멀티패스 모드, 새 CLI 옵션 |
  | 버그 수정(동작 영향) | **PATCH** (`0.0.x`) | RNLC Cauchy 교체, mud window 고갈 수정 |
  | 문서·테스트·주석만 | **버전 유지** | `[Unreleased]`에 날짜 항목만 누적 |
- 각 항목은 날짜(`YYYY-MM-DD`)와 커밋 SHA를 함께 남긴다.
- 분류 태그: `추가`(신기능) · `변경`(기존 동작 변경) · `수정`(버그) · `문서/테스트`(비기능).
- 상세 배경·원인·측정치는 `CLAUDE.md`의 "버그 수정 이력" 섹션(§번호)에 두고, 여기서는 요약 + 참조만 남긴다.
- 버전을 올릴 때: ① `common.h`의 `MULTI_FEC_VERSION` 갱신 → ② 아래에 새 버전 섹션 추가 → ③ `[Unreleased]` 항목을 그 섹션으로 이동.

---

## [Unreleased]

**제품 코드** 변경 없음 — 측정·문서·테스트 하네스만. 버전은 `1.3.1` 유지.

- 문서/테스트 · 2026-09-11 — **원거리 2단 캐스케이드 30분 소크** (`test-results/2026-09-10-v131/`
  §8, 하네스 `mf_wg_cascade_far.sh`). §7 은 추가 홉이 온링크라 **2단이 멀리 있는 경우**를
  재현하지 못했다. `r` 의 prio qdisc 에 `pref 3` 필터를 더해 `r→s` 를 기존 netem band 로
  보내 **홉마다 임피어먼트**를 걸었다 — path[0] 편도 50 ms·상향 누적 손실 ≈9.75%,
  path[1] 편도 60 ms·≈3.96%.
  **결과: 전달 99.9969%(유실 59/1,875,000), 오배송 0, 가드 트립 0.** 유실이 근거리 런의
  8배(7→59)로 늘었지만 `-f 5:1,20:4` + duplicate 가 덮는다. **RTT 80.2 ms**(1단 54.3 ·
  근거리 53.5)로 증가분 26 ms 는 단방향 임피어먼트와 일치한다 — **지연은 홉 수가 아니라
  홉의 거리가 정한다.** 자원은 세 런이 사실상 동일(릴레이 두 단 모두 RSS 변화 0회·FD 32
  고정, 클라·서버 램프 후 130.5~130.9 MB 고원).
  ⚠️ **단방향이다** — `s` 의 `ens18` 에는 운영 서버(:443)가 있어 root qdisc 를 심지 않았다.
  하향 원거리 구간과 그로 인한 지연차 확대(중복 누출)는 여전히 미재현이다.
  운영 경로용 `pref 1` 무변경, 런 후 `pref 2·3` 잔존 0·netem 기준값 유지 확인.

- 문서/테스트 · 2026-09-11 — **릴레이 2단 캐스케이드 스모크** (`test-results/2026-09-10-v131/`
  §7, 하네스 `mf_wg_cascade.sh`). 실서비스망이 `c ─ r 2단 × 2경로 ─ s` 인데 테스트망은
  1단만 돌려 왔다 — **어떤 버전으로도 측정된 적이 없던 구성**이다. 호스트가 3대뿐이라
  1단은 `r`(.85/.86:4443), 2단은 `s`(.84:4460/4461)에 올려 **두 홉을 모두 실제 회선**으로
  만들었다(c→r netem · r→s 온링크). 각 홉이 HMAC 을 검증하는 서비스와 같은 구조다.
  **결과: 홉을 하나 더 지나도 1단과 사실상 같다** — 전달 99.9996%(유실 7/1,875,000, 1단과
  동일 수치), 오배송 0, 터널 RTT 53.5 ms(1단 54.3), 가드 트립 0. 2단 릴레이 RSS 3.28 MB ·
  계단 0회 · FD 32 고정. `s` 호스트 busy 만 16.8 → 24.7% 로 오른다(2단이 s 에 얹힘).
  ⚠️ **원거리 2단의 지연은 재현되지 않는다**(추가 홉이 같은 `/24` 라 RTT 가 안 올랐다).
  ⚠️ **누수 판정 지표 보강 필요** — `analyze_leak.py` 가 계단 **크기**를 보지 않아, 말단의
  +0.01 MB(10 KB) 틱 때문에 캐스케이드 런이 "평탄 0.0분" 으로 나왔다(0.75 MB 이상 계단은
  t=1075s 에 끝났다). 계단에 크기 하한을 둘 것.

- 문서 · 2026-09-10 — **v1.3.1 적용 런북** (`test-results/2026-09-10-v131/ROLLOUT.md`).
  서비스망 구성(`c ─ r 2단 × 2경로 ─ s`)에 맞춘 호스트별 명령 — 백업·설치·재시작·역할별
  확인·롤백. 요점 셋: ① **검증에 쓴 md5 그대로 배포**(재빌드하면 빌드 시각이 버전 문자열에
  들어가 바이트가 달라진다) ② 릴레이는 **한 번에 하나씩** 재시작(2단 구성에서 서로 다른
  체인을 동시에 재시작하면 전면 단절) ③ `--auth-interval` 이 4개 홉과 c·s 에서 전부 같아야
  하며 어긋나면 그 경로가 **조용히** 폐기된다.

- 문서/테스트 · 2026-09-09 — **이중화 격리 경계(SPOF 2개) 실측** (`test-results/2026-09-09-spof/`).
  배포 판정의 미해결 항목이었고, *"경로 2개는 프로세스 장애만 분리한다"* 는 주장이 한 번도
  실측된 적이 없었다. **주장은 사실이고 릴레이 쪽은 문서보다 나쁘다.**
  **A. 릴레이 프로세스 1개 정지** → **공백 0**(응답 99.4%), RTT 54.2 → **64.2 ms** 로
  netem path[1](RTT 60ms)에 절체 — duplicate 가 설계대로 커버한다.
  **B. 릴레이 프로세스 2개 동시 정지**(호스트 장애 대리) → **연속 공백 27.94초**(응답 3.1%).
  **복구는 자동 2.8초.** → 릴레이 호스트 장애 시 예상 서비스 중단이 약 28초다.
  ⚠️ **릴레이 공유 층위가 셋이다** — 문서는 "호스트·스위치·L2" 만 적었으나 실제로는
  `.85`/`.86` 이 **같은 인터페이스 `ens18` 의 두 IP** 이고 그 NIC 은 **단일 큐**(`rx-0 tx-0`)다.
  링크 장애 하나가 두 경로를 지우고, 단일 큐는 성능 SPOF 까지 겸한다.
  ⚠️ **부수 발견 — 클라이언트 pending 큐에 TTL 이 없다(제품 결함).** 복구 직후 첫 응답
  RTT 가 **26,574 ms** 였고 이후 정확히 **204 ms(=프로브 간격)씩 감소** — 단절 중 쌓인 약
  130개가 FIFO 로 배출된 것이다. 코드 확인: `mf_server.cpp` 는 `SERVER_PENDING_TTL_MS 1000`
  과 `ts` 필드를 갖는데 **`mf_client.cpp` 의 `pending_pkt_t` 에는 시각 필드가 없다** —
  v1.0.2(§19-라)의 TTL 이 서버에만 들어갔다. 단절 복구 시 수십 초 묵은 패킷 최대 512개가
  터널에 주입되고 새 패킷이 그 뒤에 줄을 선다. **제품 코드 수정이라 적용하지 않았다** —
  제안: `pending_pkt_t` 에 `ts` 추가 + 서버와 같은 1초 TTL(와이어 무변경, 클라 단독).
  현 테스트망은 호스트가 3대뿐이라 **SPOF 해소 자체는 불가**하다(2026-08-02 §7 판단 유효).
  → `test-results/2026-09-09-spof/REPORT.md`

- 변경(하네스) · 2026-09-09 — **샘플러 온호스트 CSV 경로에 런 식별자 (`RUNID`).**
  경로가 `/tmp/<pre>_c.csv` 로 고정이라 **연속 장시간 런에서 충돌**했다 — 2026-09-07
  24시간 소크에서 1차가 17시간에 트립으로 끝났어도 샘플러 수명(24.2시간)이 남아 계속 돌았고,
  2차 샘플러와 **같은 파일에 동시 기록**해 중복 행이 생겼다. 게다가 그 잔존 샘플러 하나가
  **가드 초과를 14배로** 만들었다(임계 근처에서는 계측 도구 자신의 부하가 결과를 바꾼다).
  ```
  RUNID=${RUNID:-$(basename "${OUT:-run}")-$(date +%m%d%H%M%S)}
  ```
  하네스 7종에 적용 — `mf_wg_soak24h.sh` · `mf_wg_multi.sh` · `mf_amp_ladder.sh` ·
  `mf_soak_18m.sh` · `mf_aging_1h.sh` · `mf_multi_soak.sh` · `mf_rt_multi.sh`.
  회수 라인은 **단일따옴표에서 변수가 확장되지 않으므로** 쌍따옴표로 바꿨다.
  스모크(10세션 × 120초)로 기록·회수를 확인하고, 호스트에 남아 있던 옛 고정경로 파일
  (최대 345 KB)을 정리했다. → `test-results/2026-09-07-soak24h/REPORT.md` §5-가

- 변경(하네스) · 2026-09-09 — **가드 임계를 부하 구조별로 분리 (`GW_PROFILE`).**
  같은 임계로 두 구조를 덮을 수 없다 — `c` 전체 CPU 가 **단일 세션 각 20 Mbps 에서 max
  53.9%** 인데 **10지사 × 각 1 Mbps 에서는 상시 68~93%**(24시간 런 초과 2,329건, 전부 오탐)다.
  10세션은 프로세스 10개 합 123% + WG 암복호 10개가 얹혀 머신 부하 자체가 다르다.
  **수준이 아니라 지속성으로 가른다** — 24시간 **정상** 런의 초과 샘플을 후보 임계에 재생해
  최대 연속을 구했다: `c 전체` 는 임계 68 에서 3연속이지만 **75 이상에서는 1연속**,
  `c 1코어` 는 90~95 에서 3연속, `r 1코어`(인프라 버스트)는 95 에서 3연속.
  ```
  GW_PROFILE=single (기본)   c 전체 68 · 1코어 90 · streak 3
  GW_PROFILE=multi           c 전체 78 · 1코어 95 · streak 5
  ```
  다중 세션 하네스(`mf_wg_soak24h.sh` · `mf_wg_multi.sh`)에 `multi` 를 고정했다.
  ⚠️ **`multi` 를 단일 세션에 쓰면 안 된다** — 24M 붕괴를 아예 못 잡는다(연속 0).
  ⚠️ **09-05 보고서의 재생 검증을 정정했다.** 당시 "정상 4런 통과·붕괴 2런 트립" 이라며
  **08-07 18M 실패도 트립한다**고 적었으나 틀렸다 — 단순 **건수**로 연속성을 추정했는데
  가드는 **연속 샘플**로 트립한다. streak 카운터로 재생하면 08-07 은 **연속 2 에서 그치고,
  구 62/85 는 잡았지만 신 68/90 은 못 잡는다.** 즉 재보정은 20M 오탐을 없앤 대가로 그
  열화 탐지력을 잃었다(그 사건은 09-02 에 재현되지 않았고 원인 미규명이며, 이 가드는
  애초에 붕괴 탐지기가 아니다).
  **방법 교훈**: 임계 후보를 로그로 재생할 때 벽시계 간격·단순 건수로 연속성을 추정하지 말고
  가드의 `초과(N/M)` 카운터로 인접 샘플을 판정할 것. **트립 줄에는 그 표기가 없으므로 그 줄도
  샘플로 세야 한다** — 빠뜨리면 실제로 트립한 런이 통과로 보인다.
  → `test-results/2026-09-07-soak24h/REPORT.md` §8 · `2026-09-05-maxperf/REPORT.md` §3-다

- 문서/테스트 · 2026-09-09 — **24시간 다중 세션 소크 완주 — 문서의 마지막 미측정 항목 해소**
  (`test-results/2026-09-07-soak24h/`). 실서비스 목표 구조(지사 10곳 × 각 방향 1 Mbps,
  **세션별 WireGuard**)로 24시간. 왕복 **89,713,550 중 유실 340 = 99.999621%**,
  **오배송 0**(24시간 내내 10세션이 남의 태그를 한 번도 안 받았다), 불량 0,
  `pacing_resets=0 sendfail=0`. 4분위 평균 유실 16.5 · 13.0 · 14.2 · 13.0 — **추세 없음**.
  **누수 없음** — RSS 계단이 c 24분 · s 43분 에 끝나고 **c 23.6시간 · s 23.3시간 불변**,
  **`r` 은 24시간 변화 0회**. FD 70/32/70 고정. **CPU 는 세 호스트 모두 감소**
  (c −3.4 · r −0.3 · s −0.4 포인트). → **누수 검출 한계가 90분 → 24시간**, 상한 약 0.005 MB/h.
  **회선 72.64 Mbps**(증폭 3.63배)로 [사이징 식](test-results/2026-09-04-sizing/REPORT.md)
  예측 71.0 과 **+2.3%** 일치 — 1시간 런에서 세운 식이 24시간에서도 유지된다.
  ⚠️ **1차 시도는 17시간에 `s` 의 `apt-daily-upgrade.service` 로 끊겼다** — apt 가 돈 8초와
  가드 트립 5초가 정확히 겹친다. 2026-08-07 에 `r` 의 apt 가 측정을 죽인 것과 **같은 실패
  양식**인데, 하네스와 mask 가 `r` 만 덮고 있었다. 세 호스트 전부 apt disable+mask 하고,
  `r`·`s` 의 유지보수 타이머 8종은 **25시간 자동 복원 예약**과 함께 정지한 뒤 재실행했다.
  17시간 데이터도 온전하다(60,000,000 중 216 = 99.999640%, 오배송 0).
  ⚠️ **가드 임계는 부하 구조별로 달라야 한다** — `c` 전체가 단일 세션 20 Mbps 에서 max
  53.9% 인데 10지사에서는 **상시 68~93%**(초과 2,329건, 전부 오탐). `GW_STREAK=5` 가
  24시간을 살렸다(최대 연속 3 → 기본값 3 이면 또 트립).
  ⚠️ **하네스 결함**: 온호스트 샘플러 CSV 경로가 `/tmp/wg_c.csv` 로 고정이고 수명이 24.2시간이라
  **연속 장시간 런에서 두 샘플러가 같은 파일에 동시 기록**한다(중복 행 확인). PID 로 정리.
  부수 발견 — **잔존 샘플러 하나가 가드 초과를 14배로 만들었다**(2,000건 → 6시간당 124건):
  임계 근처 운용에서는 계측 도구 자신의 부하를 세야 한다.
  → `test-results/2026-09-07-soak24h/REPORT.md`

- 문서/테스트 · 2026-09-07 — **`r` 의 1코어 스파이크 규명 + apt 타이머 mask.**
  재보정 후 남은 유일한 초과원이 `r` 이었다. **원인은 우리 부하가 아니라 그 호스트에 얹힌
  무관한 인프라 컨테이너**(`infra-collector` · `infra-pb` PocketBase)의 **30분 주기(:23/:53)
  버스트**다. 근거: ① 가드·부하 없는 상태에서 이미 98% (성분 **usr 89 / sys 13 /
  steal 0 / softirq 0** → VM 경합·네트워크·가드 ssh 전부 아님) ② 가드가 잡은 초과가
  전부 `r` 머신 부하 급등 시각이고 **릴레이 프로세스는 51~58% 평탄** ③ cgroup 귀속에서
  `containerd` + `docker-<infra-pb>` ④ 버스트는 약 10초인데 **90% 초과는 2초**뿐이라
  `STREAK=3` 이 걸러낸다(전 런 `r` 초과 최대 연속 2) ⑤ 릴레이는 22 Mbps 에서도 코어당
  26~29% 로 포화와 거리가 멂.
  ⚠️ **정정**: `r` 이 원인이 된 트립이 1건 실재한다(`2026-08-07-aging-1h/raw`, apt-daily 가
  1코어를 33초 포화 → 3연속 초과 → `rt_multi.py` 중단). apt 는 33초라 넘겼고 인프라
  버스트는 2초라 아직 못 넘긴 것뿐이다.
  **조치(사용자 지시)**: `r` 의 `apt-daily.timer`·`apt-daily-upgrade.timer` 를 **disable +
  mask**. mask 까지 한 이유는 소크 하네스가 정리 단계에서 후자를 다시 `start` 하기 때문이다.
  되돌리기 명령과 "패치가 수동이 된다" 는 경고를 REPORT §3-마 에 남겼다.
  **검증**: apt mask 후 20 Mbps 1시간 재실행 — **완주 · 트립 0**, c→s **0.00071%**(역대 최저),
  RSS 세 호스트 변화 0회. 그런데 **`:53` 과 `:23` 초과가 모두 재현**됐다 →
  **apt 가 아니라 인프라 컨테이너임이 확정**.
  → `test-results/2026-09-05-maxperf/REPORT.md` §3-마

- 문서/테스트 · 2026-09-07 — **각 22 Mbps 지속 가부: 동작하나 권고 불가.**
  재보정된 기본 임계(68 / 90)로 1시간 소크를 시도해 **38분 24초** 를 갔다.
  그 사이 **손실·누수·드리프트가 전혀 없다** — c→s 0.0125%(5분 구간 0.0107~0.0145% 평탄),
  실효 22.00 고정, RSS 세 호스트 모두 변화 0회, FD 고정, c 프로세스 CPU 는 워밍업 후
  83.7 → 82.6 으로 오히려 하락. 24 Mbps 와는 성질이 다르다(24 는 169초에 한 초의 15.1% 소실).
  **그런데 `c` 1코어가 90.1~91.8%(p50 90.2)로 임계 위에 상주한다** — 초과 72건 중 70건이
  이것이고, 20 Mbps 의 대역(85.1~87.5)과 달리 임계를 걸치고 앉는다. `multi-fec` 은 단일
  스레드라 **1코어 100% 가 절대 상한**이고 프로세스가 이미 코어의 83.4% 를 쓴다(여유 17p).
  **→ 지속 상한은 20 Mbps 유지.** 22 는 "손실은 없지만 여유가 없다" 로 기록한다.
  ⚠️ **이 트립을 없애려고 임계를 또 올리지 말 것** — 90 → 95 는 실제 병목(단일 코어)의
  마지막 가드레일을 치우는 것이다. 20 의 오탐 해소(85 → 90)는 정상 대역이 임계 아래에
  명확히 있어서 정당했지만, 22 는 **대역 자체가 임계 위**다. 계측 문제가 아니다.
  → `test-results/2026-09-05-maxperf/REPORT.md` §2-나

- 문서/테스트 · 2026-09-06 — **재보정된 가드 임계 실측 검증.**
  환경변수 없이 **기본값(전체 68 / 1코어 90)** 으로 20 Mbps 1시간을 다시 돌려
  **트립 0 · 초과 4건**으로 완주했다. c→s **순손실 0**/7,211,537 · s→c 0.0025%,
  실효 양방향 20.0 고정, RSS 는 세 호스트 모두 **변화 0회**, FD 고정.
  **`c` 전체 CPU 는 한 번도 임계를 넘지 않았다**(p50 51.3 / max 53.9, 임계 68 대비 14p 여유)
  — 1차 판별자로 삼은 근거가 실측으로 확인됐다. 초과 4건 중 c 관련은 **종료 6초 전 1건**뿐이고
  나머지 3건은 전부 `r`(프로세스 CPU 는 51~52% 평탄, 미규명 — REPORT §5-5).
  같은 20 Mbps 를 구 임계(62/85)로 돌리면 32분간 **초과 106회 + 트립**이었다.
  → `test-results/2026-09-05-maxperf/REPORT.md` §3-라

- 변경(하네스) · 2026-09-05 — **`mf_gwguard.sh` 임계 재보정: 전체 62 → 68 · 1코어 85 → 90.**
  이전 값은 **45초 계단**에서 정했는데 계단은 CPU 를 과소평가한다(같은 각 22 Mbps 가 계단
  72.8% vs 지속 소크 연속 샘플 85~90%). 그래서 지속 부하의 정상 구간에 임계가 붙어
  **앱 각 20 Mbps 가 32분간 아무것도 나빠지지 않았는데 트립**했다(그 사이 초과 106회).
  **지속 소크의 1초 샘플**로 다시 정했다 — 정상 런의 초과 샘플은 전체 ≤64.3 · 1코어
  85.1~87.5(88 이상 0회)인데, **붕괴 24M 은 전체 72.0~76.1 · 1코어 90.6~94.2**,
  실패 18M(08-07)은 전체가 62.4 → 73.5 로 상승했다. **`c` 전체가 진짜 판별자**이고
  1코어는 보조다(종료 전환에서 93.2 가 한 번 나와 여유가 좁다).
  **기록된 로그 재생 검증**: 정상 4런(16M·18M·20M 32분·20M 1시간) 전부 통과 —
  그중 20M 32분은 **트립 → 통과**로 오탐이 해소됐다 — 붕괴 24M(전체 3회·1코어 4회)과
  실패 18M(전체 7회·1코어 4회)은 트립. r·s 전체 75 와 링크 400 Mbps 는 무변경.
  ⚠️ **가드는 붕괴 탐지기가 아니다** — 24M 붕괴는 60초 샘플에서 1코어가 78~80% 로
  **정상 20M(86.9%)보다 낮게** 보였다. CPU 로 예고되지 않고 손실이 먼저 터진다.
  손실 SLO 는 수신측 초 단위 계측으로 봐야 한다. 상세 `2026-09-05-maxperf/REPORT.md` §3-다.

- 문서/테스트 · 2026-09-05 — **최대 성능 재측정: 지속 상한 18 → 20 Mbps**
  (`test-results/2026-09-05-maxperf/`). `c` 의 링크가 100 → 1000 Mbps 가 된 뒤 처음 잰다.
  **버스트 한계는 안 올라갔다(각 22~24)** — 링크를 10배로 키워도 불변이므로 병목은
  링크가 아니라 **`c` 의 단일 코어**로 확정. 다만 08-06 의 "34 Mbps 붕괴" 는 링크
  바운드였음이 드러났다(1 GbE 에서 실효 33.8M 달성, 손실 14% → 0.59%).
  **지속 소크 3종**: 각 24 는 **2분 57초에 붕괴**(150초까지 완벽하다가 169초에 한 초의
  15.1% 가 소실) · 각 22 는 3회 모두 19~24초에 트립하나 **손실 0** (코어가 83.5% 로
  여유가 없을 뿐) · **각 20 은 1시간 완주** — c→s 0.0015%, s→c 0.0019%, 실효 20.0 고정,
  손실이 시간에 따라 커지지 않고 오히려 후반 20분은 10분에 1패킷. **RSS 는 세 호스트
  모두 1시간 동안 1 KB 도 안 변했고** FD 고정, CPU 는 워밍업 후 평탄·후반 하락.
  회선 증폭 2.69배(앱 40 → 107.5 Mbps)로 사이징 식과 일치.
- 변경(하네스) · 2026-09-05 — **`mf_gwguard.sh` 에 ssh ControlMaster 적용.**
  샘플마다 ssh 를 새로 맺어 **피측정 호스트에서 sshd + 키교환 비용**이 발생했다. 트래픽 0
  A/B 실측: c 전체 busy 0.5% → **11.5%**(가드만 켜고), 코어당 +8~17p. 실제 1코어가
  78~84% 인 구간에서 이 오버헤드가 얹혀 **손실 0 인데 트립**했다. ControlMaster 로
  연결을 재사용해 **11.5% → 4.2%**(코어당 3.6~4.7p). **임계값·판정 로직 무변경**,
  백업 `mf_gwguard.sh.bak-precontrolmaster`.
  ⚠️ **1코어 임계 85 의 근거가 흔들렸다** — 20 Mbps 에서 32분간 손실·CPU·메모리가
  전혀 나빠지지 않았는데 트립했다(그 사이 단발 초과 107건). `GW_CORE=90` 으로는 초과
  8건에 1시간 완주. 85 는 08-06 계단(3회 평균)으로 정했는데 **계단은 CPU 를
  과소평가한다**(같은 22 Mbps 가 계단 72.8% vs 연속 샘플 85~90%). **재보정은 하지
  않았다** — 90 은 34 Mbps 붕괴를 놓친 값이라 별도 판단이 필요하다. §5 참조.
  ⚠️ **측정자 과실 2건 기록**: 30 트립 후 하네스의 문자열 기반 가드 생존 검사가
  종료 중 프로세스를 매치해 **34 단계가 가드 없이 돌았다**(피해 없음, 절차 위반).
  그리고 `pkill -f "패턴"` 이 자기 셸을 죽여(exit 144) 가드가 누적되는 함정에
  **하루 3회** 걸렸다 — 정리는 반드시 `ps` → **PID kill** 로 할 것.

- 문서 · 2026-09-05 — **자원 누수 원자료 재분석** (Outline `/doc/RHeI4DhXD3` §3-가).
  보고서 수치 인용이 아니라 각 런의 샘플러 CSV 를 다시 계산했다. **RSS·CPU·FD 누수 없음** —
  5시간 소크는 c·s 의 RSS 가 **변화 0회**(릴레이는 −17 KB/h 감소), 12·14·16·18 Mbps 1시간은
  전부 램프조차 없이 13.2 MB 불변, 90분 10세션은 **첫 9분에 증가분 +19.4 MB 중 +18.8 MB 가
  집중**되고 이후 c 는 마지막 47분간 1 KB 도 안 움직였다. 증분이 **0.12/0.25/0.50 MB
  (=128 KB 배수)** 에만 찍혀 glibc allocator arena 확장이지 누수가 아니다(프로세스당 1.94 MB).
  CPU 도 1구간→2구간 워밍업(+1.9~+4.4p) 뒤 평탄하고, **최장 5시간 런에서는 오히려 −1.4p** 다.
  FD 는 전 런 완전 고정(8 · 14 · 64~70).
  ⚠️ **검출 한계**: 마지막 평탄 구간에서 128 KB 계단조차 안 밟았으므로 상한은
  **프로세스당 < 16 KB/h**. 하루 4 MB 미만 누수는 90분·5시간 관측으로 검출 불가 —
  24~72시간 소크가 남은 이유다.
  ⚠️ **하네스 함정 신규(19번)**: 샘플러 CSV 를 그대로 회귀하면 부하 종료 후 행이 섞여
  `2026-08-03` 8세션 런이 **−73.97 MB/h · CPU −159%** 로 나온다. `nproc` 이 모드값인 행만
  남겨야 정상값(+0.00 MB/h · 55.1%)이 된다. **기동·종료 구간을 안 자르면 누수 판정이 뒤집힌다.**

- 문서 · 2026-09-05 — **테스트 종합 색인 신설** (Outline 전용, 리포 변경 없음).
  지금까지 수행한 검증을 한 문서로 모았다 — 코드 레벨 스위트 12종(90/90 · 26/26 · 24/24 등),
  실토폴로지 측정 12건(2026-06-18 8시간 에이징 ~ 2026-09-04 사이징), 테스트로 확정된 수치,
  **측정이 먼저 잡아낸 제품 결함 6건**(v1.0.4/1.0.5/1.0.7/1.0.8/1.0.9/1.3.0), 하네스 교훈 18건,
  남은 미측정 8건. 각 항목은 기존 측정 문서로 링크만 걸고 **개작하지 않았다**.
  Outline `/doc/RHeI4DhXD3` — 인덱스 `19gMJ0BBeC` 하위 + `핵심 문서` 표에 ★ 로,
  성능 요약 `5bkOkgupB0` 에는 서두·§7·근거문서 3곳에 링크. 인덱스 링크 25개 전수 확인(깨짐 0).
  ⚠️ 39리비전짜리 인덱스에 한 줄을 넣을 때 **MCP `update_document`(전문 교체)로 손수 옮기지 말 것** —
  REST `documents.info` → 로컬 문자열 치환 → `documents.update` 로 왕복하면 원문이 보존된다.

- 문서/테스트 · 2026-09-04 — **90분 소크: CPU·메모리 누수 없음** (`9efe20e`).
  10지사 구성(세션별 WG)으로 90분. 왕복 **99.9997%**, 오배송 0. RSS 는 램프 이후 70분 동안
  **계단 2개**뿐이고 **c 는 마지막 47분, s 는 마지막 30분 동안 1 KB 도 변하지 않았다.**
  FD 전 호스트 고정. CPU 는 6구간에서 워밍업 후 평탄(마지막 두 구간은 하락).
  ⚠️ **누수를 선형회귀로 판정하면 틀린다** — 부하 생성기(+0.000±0.000)와 릴레이(감소)에도
  "유의" 가 나온다. **계단 개수와 마지막 평탄 구간**으로 봐야 한다.
  한계: 90분으로는 시간당 0.1 MB 미만 누수를 검출할 수 없다(24~72시간 미시험).
  → `test-results/2026-09-02-multisession/REPORT.md` §3-바

- 문서/테스트 · 2026-09-04 — **회선 사이징 식 재도출** (`aa298e7`).
  증폭이 단일 계수가 아니라 **레이트의 톱니 함수**임을 9점 실측으로 확인하고 식을 세웠다:
  `세션당 회선 = 4.349 × FEC(R) × R + 0.580`, `총 회선 = N × (그 값)`.
  적합 오차 최대 1.55%, **다른 런 4점 교차검증 −1.2%~+0.9%**. 구식 `5.34 × R × N` 은
  R ≥ 3 에서만 맞았고 지사당 1 Mbps 에서 **1.33배 과소**였다. `g` 는 트래픽이 정하는데
  `y` 는 계단이라 R=4→5 에서 레이트 25% 인상에 회선이 38% 는다.
  → `test-results/2026-09-04-sizing/REPORT.md`

- 문서/테스트 · 2026-09-02 — **세션별 WireGuard 다중 세션 소크** (`16b0736`).
  지사마다 독립 WG 터널을 세워 실서비스 구조(10지사 × 1 Mbps)를 1시간 재현했다.
  왕복 **99.9995%**, **오배송 0**, 누수 0. 같은 날 오전의 4×4 런은 **WG 가 없어**
  처리량·CPU 값을 폐기했다(사용자 지적으로 발견). 하네스 결함 3건 수정 — 가드 트립 후
  무감시, 샘플러 패턴의 운영 프로세스 합산, 패턴·cmdline 불일치.
  → `test-results/2026-09-02-multisession/REPORT.md`

- 문서/테스트 · 2026-08-31~09-02 — **18 Mbps 까지 지속 통과, 08-07 붕괴 미재현** (`77e0c5f`).
  12·14·16·18 을 각 1시간 소크해 전부 통과(18 은 c→s 순손실 0). **2026-08-07 의
  "18 은 21분 12초에 붕괴" 는 재현되지 않았고 원인 미규명이라 한계의 근거로 쓸 수 없다.**
  09-02 오전에 세웠던 "지속 한계 16<X<18" 과 "CPU 드리프트 단조증가" 두 주장을 취소했다.
  12일 정지의 원인은 **시계 드리프트**였다(부팅 시 유닛 24개 미기동 → chrony 미동작 →
  +73.9초 → mud timetolerance 30초 초과 → 무음 폐기).
  → `test-results/2026-08-31-postfix-recheck/REPORT.md`

- 문서/테스트 · 2026-09-04 — **v1.1.0 · v1.2.0 · v1.3.0 태그 소급 부착**.
  코드 버전은 1.3.0 인데 git 태그가 `v1.0.9` 에서 멈춰 있어 `git describe` 가
  `v1.0.9-28-...` 로 나왔다. v1.0.6~1.0.9 와 같은 방식(각 릴리스의 마지막 커밋에
  annotated tag)으로 붙였다 — v1.1.0 → `143dc2a`, v1.2.0 → `2be8cfd`,
  v1.3.0 → `2963949`.

- 문서/테스트 · 2026-08-07 — **지속 부하 소크: 45초 계단 값은 지속되지 않는다**
  (`test-results/2026-08-07-aging-1h/`). 2026-08-06 계단이 준 "실용 한계 각 방향 22 Mbps"
  는 **단계당 45초** 값이라 SLA 로 쓸 수 있는지 확인했다.
  **각 방향 18 Mbps 는 21분 12초에 붕괴** — 실효는 17.98 Mbps 를 끝까지 유지하는데
  **손실이 전반 0.051% → 후반 0.218%(4.3배)** 로 커지고 c 전체 CPU 가 56%→65% 로 드리프트해
  가드가 트립했다(초과 37건 중 30건이 `c:전체>62%`). 열은 배제(26°C, thermal event 0건,
  governor schedutil, bios_limit 정격). **각 방향 12 Mbps 는 1시간 완주** — c→s 순손실
  **0/4,326,923**, s→c 74/4,326,922(0.0017%), CPU 42.7→43.0% 평탄, 가드 트립 0.
  → **지속 운용점 각 방향 12 Mbps 확정**(양방향 24, 회선 66 Mbps). 지속 한계 12<X<18 은 미측정.
  **메모리·FD 누수 없음** — 저부하 8세션 48분(마지막 30분 +0.00/+0.00/+0.33 MB/시간, FD
  64/42/14 고정)과 고부하 1시간(전 호스트 +0.00 MB/시간, FD 고정) 양쪽에서 확인. 누수 판정은
  **마지막 30분 기울기**로 해야 한다 — 램프가 약 20분이라 전반으로 판정하면 오판한다.
  **트래픽 격납**: 3개 런 전부 시험 데이터가 테스트망 안에만 있고 외부 인터페이스는
  ±0.000 Mbps(2026-08-07 신설된 c 의 LTE `enp3s0` 포함). 증폭 2.63배.
  **하네스 결함 4건 발견·수정**(REPORT §7) — 그중 정리 패턴을 `518[6-9]`→`518` 로 줄여 쓴
  것이 **운영 클라이언트(:51821)를 20분 정지**시켰고, bracket 패턴이 *같은 커맨드라인
  뒤쪽의 평문*에 무력화되는 새 변종도 확인했다. 상세는 `CLAUDE.md` 규칙 0 절과 REPORT.
- 문서 · 2026-08-07 — **c 의 `enp3s0` LTE 회선 영구화.** netplan 고정 IP `192.168.3.101` +
  `table 103` 정책 라우팅(main 무영향, Zoom 48개 프리픽스·테스트망 라우팅과 비경쟁),
  `enp3s0-oif-rule.service`(netplan 의 routing-policy 가 `oif` 미지원). 기존에는 DHCP 리스를
  못 받은 채 주소만 수동 추가돼 있어 재부팅 시 소실되고 경로가 아예 없었다. `CLAUDE.md`
  라우팅 표에 반영.

- 문서/테스트 · 2026-08-06 — **`s` 리슨 이전 후 첫 부하 시험** 통과
  (`test-results/2026-08-06-postmove-loadtest/`). 앱 각 방향 4 Mbps UDP 60초, netem
  5%/2% 하에서 **앱 손실 0/24,039 · 0/24,040 (0.000%)** — duplicate 두 사본과 FEC 가 전부
  복구했다. WG RTT 53.21 ms(유휴 54.08)로 열화 없고 경로 2개 전 구간 RUNNING.
  **새 부하 지표 검증**: `budget` 예측 c CPU 22% vs 실측 23.2%/28.7% → 모델이 15~18%
  낙관적(용량 모델 메모의 경고 방향과 일치). 가드 초과·트립 0건. 지표 산식도 CPU 점유로
  대조(1코어→전체 27.1%/최고 100%, 2코어→51.0%). **증폭 2.75배** 분해:
  duplicate 2 × FEC 1.228(서버 report 실측) × obfs·헤더 1.068.
  **트래픽 격리 확인**: 전 인터페이스 델타로 시험 데이터가 전량 `192.168.100.0/24` 위에만
  있음을 확인. `s ens19`(공인)에 있던 것은 수동 캡처 전수 ISP ARP·VRRP 와 **가드 자신의 ssh
  폴링**(16 kbps)뿐 — 감시 경로가 외부를 지난다는 점을 기록했다.
  **미해결 계측 이상 2건**: ① 클라이언트 report 의 `rtt=26us/31us` 가 netem 편도값
  25/30 ms 와 숫자는 맞는데 단위 표기가 µs ② 보고 손실률이 netem 과 불일치
  (path[0] 7~14% vs 5%, **path[1] 0~1% vs 2%** — 후자는 in-flight 팽창으로 설명 안 됨).
  둘 다 표시·계측 문제로 보이고 데이터 경로 동작 영향은 관측되지 않았다.
- 문서/테스트 · 2026-08-06 — **계단 시험으로 실용 한계 재측정**
  (`test-results/2026-08-06-postmove-loadtest/` §9–11, `mf_ladder_udp.sh` 추가).
  **실용 한계가 앱 각 방향 8 → 22 Mbps 로 2.75배 올랐다.** 옛 8 Mbps 는 CPU 가 아니라
  **gw 바운드**였다(gw 70 Mbps ÷ 증폭 3.54 ≈ 각 8 — 산수가 맞는다). gw 가 경로에서 빠지자
  각 22 Mbps 까지 손실 0.034% 이하로 나가고, **붕괴는 각 34 Mbps**(최고1코어 85.7%, 실효
  미달, 손실 14%)에서 온다 — 단일 스레드 천장이다. 무릎점에서 c 56.9% / r 19.2% / s 12.6% 로
  **병목은 c 단독**이다.
  **용량 모델 보정**: 12 Mbps 한 점의 선형 외삽이라 R=44 에서 예측 118% vs 실측 56.9% 로
  **2.1배 과대**하다(비용이 sublinear — FEC 그룹이 커져 패킷당 패리티가 줄고 recvmmsg/sendmmsg
  배치가 잘 채워진다). 제약 ②(15.9)·③(31.25)도 R=44 를 깨끗이 통과했다. **모델은 12 Mbps
  근방에서만 유효**하므로 `budget` 판정은 재도출 전까지 참고용이다.
  **⚠️ 가드 임계가 느슨하다**: 붕괴 단계에서 손실 14% 인데 최고1코어 85.7% 가 임계 90% 에
  못 미쳐 **트립하지 않았다.** `GW_CORE=80` 하향을 권하되 26–30 단계가 "동작하지만 손실 있음"
  구간이라 운용 목표에 달렸다 — 미결정.
  **트래픽 격리 재확인**: 10초 창을 **60초 전 구간 누적**으로 바꿔 재측정(`ifbytes.sh` 추가,
  가드를 양 구간에 켜 폴링을 상쇄). 공인 인터페이스 기여 **RX 20 KB / TX 0**(0.024%),
  총량 수지에 설명되지 않는 트래픽 없음.
  **`s` 를 c 와 같은 하드웨어로 바꿀 때 추정 약 57%** — 실측 성능비(ChaCha20-Poly1305
  1024B: c 99.1 vs s 427.3 MB/s = 4.31배)로 환산. c 실측 56.9% 와 거의 같아 **한계는 거의
  안 움직인다**(두 끝점 작업이 대칭). 단 s 는 FEC 디코드라 손실이 커지면 더 빨리 는다.
  부수: 1초 창 CPU 샘플의 창간 편차가 **±5–9 포인트** — 단일 샘플로 판단하지 말 것.
- 문서/테스트 · 2026-08-06 — **TCP goodput + RPS A/B** (같은 리포트 §12–13, `rps_ab.sh` 추가).
  **TCP 가 UDP 용량의 95% 를 낸다**: 상향 37.9 / 하향 19.5 / 양방향 41.9 Mbps (UDP 44).
  FEC·duplicate·재정렬이 TCP 를 망가뜨리지 않았다 — `지연차÷RTT = 5/54 = 0.09` 로 작고
  재전송도 137 MB 에 278건(0.25%)이다.
  **⚠️ iperf3 `--bidir` 은 TCP 에서 못 쓴다**: 합계 1.8 Mbps(재현 시 0.24)로 36배 붕괴하는데,
  **별도 인스턴스 2개는 41.9 Mbps** 다. `-i 2` 인데 구간 보고가 10초로 나온 것이 제어 연결
  정지의 증거이고 UDP `--bidir` 은 정상이었다 → iperf 3.9 TCP 경로 artifact. 하마터면 도구
  결함을 제품 병리로 보고할 뻔했다. **앞으로 TCP 양방향은 별도 인스턴스 2개로 잰다.**
  **RPS 는 개선이 없다**: `rps_cpus` 0/3/e 에서 하향 20.5/18.9/19.9 Mbps 로 시행 편차(±2) 안,
  `cpu2` 도 42~48% 로 불변 → `rps_cpus=0` 유지. **"하향 병목 = softirq cpu2 고정" 귀속을
  철회한다** — 어느 코어도 포화하지 않았고(최대 48%) 풀어줘도 안 오른다. 플로우가 2개뿐이고
  비싼 일이 multi-fec 단일 스레드에 있기 때문으로 보인다.
  **하향이 상향의 절반인 원인은 미규명**으로 남는다(`-P 4` 가 단일보다 낮아 TCP 제어 루프도
  아니다). mud `rate`/`window` 가설은 역방향 2 Mbps 주입에서 +11% 로 부호는 맞았으나
  편차 안이라 **확인되지 않았다**.
  부수: `GW_CORE=85` 로 조인 가드가 유휴 구간의 `s` 1코어 100% 를 실제로 잡았다 — 기본 90
  이었다면 못 잡았을 값(§9-다 하향 권고를 지지).
- 변경 · 2026-08-06 — **`mf_gwguard.sh` 최고1코어 기본 임계 90 → 85.** 기본값 90 이 붕괴를
  놓쳤다(각 방향 34 Mbps, 손실 14%, 실효 미달인데 1코어 85.7% 로 트립 안 함). 80 은 정상
  동작 구간(26·30 단계의 79.1~79.2%)에 붙어 있어 1초 창 편차 ±5~9 포인트로 오탐한다.
  85 는 갈림이 분명하고 TCP·RPS 시험에서 붕괴급 85.7% 와 유휴 구간 `s` 1코어 100% 를 실제로
  잡으면서 건강 구간 오탐이 0 이었다. 근거 표를 스크립트 헤더와 `CLAUDE.md` 규칙 0 에 남겼다.
  **이 임계는 "붕괴를 멈추는" 선이지 "손실 0 을 지키는" 선이 아니다** — 26~30 구간은 동작하되
  손실 0.1~0.8% 이고, 손실 SLO 는 부하를 낮춰야 하지 가드로 대신할 수 없다.

- 문서/테스트 · 2026-08-05 — **테스트망 `s` 리슨을 `192.168.200.254` → `192.168.100.84` 로
  이전** (코드 변경 없음, 설정만). `ens18` 에 `.84/24` 를 추가해 c·r 과 같은 `/24` 에 올리고
  구 정적 라우트 `192.168.100.0/24 via 192.168.200.1` 을 제거했다. **데이터 경로 전체가
  온링크가 되어 gw 가 경로에서 빠졌다** — MAC 으로 확정(`r ens18 → s ens18` 직결, gw MAC
  미출현). ⚠️ **`mf_gwguard.sh` 의 대리지표(`s ens18` RX+TX)가 무효화**됐고 70 Mbps gw 한도가
  이 체인을 제약하지 않는다. 부수로 **gw 의 SNAT 가 우회**되어 `s` 가 릴레이 실제 IP 를 직접
  본다(v1.1.0 `--upstream-local` 의 효과가 `s` 에서 관측 가능해짐). 새 기준 RTT: s→r `.85`
  **0.53 ms**(구 1.00), s→r `.86` 0.64, s→c 1.12(구 1.30), WG 터널 **54.1 ms**.
  검증: 규칙 2 12조합 위반 0, WG 왕복 손실 0, netem 은 `r↔s` 미적용(필터가 `dst .141` 매치).
  함께 정정한 문서 오류 3건 — **gw 는 "NAT 없음" 이 아니다**(서브넷 간 SNAT 실측),
  `c` 의 `enp3s0` 는 `192.168.1.214` 가 아니라 `192.168.3.100`,
  **`r` 만 타임존이 UTC** 라 호스트 간 로그가 9시간 어긋나 보인다.
- 문서 · 2026-08-05 — **규칙 0 재작성**: "가드가 무효" 경고를 붙이는 대신 사실관계로 정리했다 —
  **테스트망은 c·r·s 가 전부 `192.168.100.0/24` 인 단일 네트워크**이고, c·r 의 default 가 gw 여도
  **longest prefix match** 로 `/24` 온링크가 먼저 걸려 테스트망 목적지에는 default 가 조회되지
  않는다. gw 가 나르는 것은 테스트망 밖으로 나가는 관리 트래픽뿐이다. 70 Mbps 예산과
  `mf_gwguard.sh` 는 무효이며 새 지표는 미정. 절차 교훈(가드 먼저 · 대리지표 실측 대조)은 유지.
  아울러 **r↔s 헤어핀의 원인을 규명**했다 — gw 는 one-armed router 이고 `192.168.100.1` 과
  `192.168.200.1` 이 **같은 MAC**(`bc:24:11:bb:7b:9c`, r·s 양쪽에서 확인)이라 서브넷 간 패킷이
  한 포트로 들어와 같은 포트로 나갔다(패킷당 2회 계상). 물리적으로는 c·r·s 가 처음부터 같은
  L2 였으므로 우회를 만든 것은 배선이 아니라 `s` 만 다른 서브넷에 둔 IP 주소 배치였다.
- 문서/테스트 · 2026-08-05 — **`mf_gwguard.sh` 재작성: 새 부하 지표**. gw 대역폭 대리지표를
  버리고 **제약을 직접 측정**한다 — `c` 전체 CPU 62%(실측 포화점) / `r`·`s` 75% /
  **최고 1코어 90%**(단일 스레드 + softirq cpu2 고정, 대개 이게 먼저 걸린다) / iface 400 Mbps
  (증폭 루프 백스톱). 세 호스트를 모두 보아 병목이 옮겨가도 눈이 멀지 않게 했다.
  `precheck` 를 추가해 **가드가 자기 전제를 먼저 검증**하고(호스트·인터페이스·온링크 12조합)
  전제가 깨지면 감시를 시작하지 않는다 — 무효한 지표로 "감시 중"을 표시하는 것이 두 사고의
  구조였다. `budget` 은 용량 모델(`N×0.0181 + 0.1067×ΣR`)로 세 제약을 각각 판정한다.
  **함께 고친 실동작 결함**: `watch` 의 로컬 kill 루프가 **자기 자신을 죽여** 원격 kill 에
  도달하지 못했다. bracket 패턴은 원격 셸만 보호하고 가드 자신의 argv 에는 패턴이 평문으로
  들어 있기 때문이다. 부하 생성기는 `c`(원격)에서 도므로 **가드가 부하를 못 멈추는 상태**였다.
  A/B 실측: 조상 PID 제외 미적용 시 원격 표적 **생존** + guard rc=137(자기 SIGKILL),
  적용 시 원격 표적 종료 + rc=2. 지표 산식도 대조했다(c 1코어 점유 → 전체 27.1%/최고 100%,
  2코어 → 51.0%). 실제 부하 하 대조는 첫 런의 첫 30초에 할 것.

- 문서/테스트 · 2026-08-03 — **경로 상관도 측정 완료 + 터널 하부망 라우팅 루프 제거**
  (`test-results/2026-08-03-pathcorr-relaycut/`). ① **두 경로는 독립**이다 — 30만 페어
  2×2 분할표에서 양쪽 동시 손실 실측 0.09900% vs 독립가정 0.09953%, **φ = −0.00017**.
  `duplicate` 의 `p = p1 × p2` 예측이 유효하다(단 netem 이 경로별 독립 주입한 환경이라
  실환경 POP 에는 그대로 옮길 수 없다). 부수로 **`r→s` 공유 구간 손실 0** 확인.
  ② 측정 중 gw `ens18` 이 800 Mbps 를 넘어 중단됐고, 추적 결과 **WireGuard PostUp 이 터널의
  하부 전송망을 터널 안으로 라우팅**하고 있었다 — c 는 `192.168.200.0/24`(s 실제 IP 포함),
  s 는 `192.168.100.0/24`(릴레이 `.85`/`.86` 포함). 터널 엔드포인트가 `127.0.0.1:51821`
  = multi-fec 자신이라 **한 바퀴마다 `duplicate` 로 2배** 증폭되는 루프가 된다.
  양쪽을 gw 경유로 교체(런타임 + netplan + WG hook)하고 **12개 조합 전수 확인**.
  s→릴레이 RTT 55 ms → **1.00 ms**, c→s RTT **1.30 ms**. 코드 변경 없음.
  ③ **netem u32 필터가 `dport 443` 까지 매치**한다는 것을 발견 — 다른 포트 프로브는
  임피어먼트를 못 받아 측정이 조용히 무의미해진다(`CLAUDE.md` 에 경고 추가).
  ④ **릴레이 단절 절체시간 측정 완료** (REPORT §2, 4종 × 36,000 패킷) — 오래 "미측정"으로
  남아 있던 항목이다. **`duplicate` 는 절체 공백이 없고**(유실 6·8개, RTT 무변화, `stop` 과
  `blackhole` 이 구분되지 않음), **`aggregate` 는 약 1초 공백**이 있다(유실 110·132개,
  `+225ms` 에 70% 손실 정점 후 ~1초 회복). 이 1초가 `MUD_PATH_DEAD_TIMEOUT`(§21-가, v1.0.4)과
  일치해 **죽은 경로 배제의 실토폴로지 확인**이 됐다(v1.0.4 이전이면 60초 내내 절반 유실).
  단절 중 손실은 모드 무관 1.20~1.26% 로 수렴하고 이론값 1.40%(g=3, y=1, 단일경로 5%)와
  맞는다. baseline 은 duplicate **0.0000%** vs aggregate 0.62~0.77% — ①의 경로 독립성이
  실물로 확인된 셈이다. `aggregate` 측정 후 `duplicate` 로 원복(자동 원복 예약 후 해제).
  1차 시도는 하네스 `pkill` 자기자신 매치로 무효였고, `rc_run.sh` 결함 6건을 고쳐 재측정했다
  (절대 epoch 예약 — `AccuracySec` 를 안 주면 systemd 기본 정확도가 **1분**이라 절단이 최대
  60초 늦게 발동한다; 프로브가 자기 시간축에 마커를 남겨 호스트 간 시각 비교를 제거).
  **`mf_gwguard.sh` 에도 같은 자기kill 결함이 있어 함께 수정** — 가드가 부하를 못 멈추면
  가드가 없는 것과 같다(②와 같은 구조).
  ⑤ **테스트망 설정 고정** (REPORT §5) — 라우팅만 고쳐도 재부팅을 못 견디면 의미가 없어
  기준선을 고정했다. c·s 의 `multi-fec-*`·`wg-quick@starlink-fec` 가 **`disabled` 상태로
  수동 기동만 돼 있어 재부팅 시 터널 체인이 안 올라왔다**(다른 기동 경로 없음 확인) → `enable`.
  r 의 `.85` 는 DHCP 산물이라 gw 가 다른 장비에 배정하면 릴레이 `LISTEN` 바인딩 실패로
  path[0] 이 사라지고, DHCP 무응답 시 default route 까지 잃어 두 경로가 동시에 죽는 구조였다
  → **static 전환**(21주간 리스 갱신 ~3,650회 동안 `.85` 유지돼 예약 가능성 높음).
  전환 검토에서 걸러낸 것: netplan 키 병합 때문에 `dhcp4: false` 명시 필요, 리스가 주던
  주소/넷마스크/ROUTER/DNS 전수 대체, `UseMTU=true` 가 있어 **MTU 확인 필수**(리스에 옵션
  없어 1500 유지 — 있었다면 `--mtu 1350` 전제가 흔들렸다). r 은 `ens18` 로만 접근 가능하고
  콘솔이 없어 **180초 자동 원복 예약 + detached apply** 안전망을 걸고 적용했다
  (`netplan try` 는 TTY 확인이 필요해 비대화형에서 쓸 수 없다).
- 문서 · 2026-08-03 — `CLAUDE.md` 테스트 환경 절 현행화. 서버 표기를 "공인 218.154.1.134"
  에서 **사설 `192.168.200.254:443` 전용 바인딩**으로 정정하고, 2026-08-02 개편된 2경로
  릴레이 구성·gw 대리지표·`mf-netem.service` 기반 netem 을 반영했다(기존 기술은
  폐기된 `s ens19` 단방향 구성이었다).
- 문서/테스트 · 2026-08-03 — **실토폴로지 다중 클라이언트 검증 완료**
  (`test-results/2026-08-03-multiclient/`). 서비스 가부 판단 항목이었다. 8세션 1시간,
  왕복 750,008 중 **손실 4(0.00053%)·오배송 0**, RSS **램프 20분 후 기울기 정확히 0**(누수 없음),
  FD 고정. 연결당 메모리 모델(서버 10.55 MB, 클라 12.89 MB)이 실토폴로지에서 확인됐다.
  하네스 4종(`mf_rt_multi.sh`/`rt_multi.py`/`rt_echo.py`/`rt_sample.sh`) 추가 — 기존
  `aging_rt_*.py` 가 서버 sink 도착분(업스트림)만 보던 공백을 **왕복으로 하향까지** 메운다.
- 테스트 · 2026-08-03 — **`mf_gwguard.sh` 결함 2건 수정.** ① `sample()` 이
  `ssh → sleep N → ssh` 후 **N 으로 나눠** ssh 왕복(~0.6s)만큼 과대보고했다(1초 창에서 1.6배).
  실제 19.4 Mbps 를 31.5 로 읽어 워치독이 허위 트립했다 → 원격에서 경과시간을 실측해 나눈다.
  ② 워치독이 **부모만 죽여** 자식 multi-fec 이 고아로 남았고, `SO_REUSEPORT` 때문에 그 고아가
  다음 런의 리슨 포트를 나눠 가져 **세션 절반이 조용히 죽는** 오염을 3런 연속 만들었다
  → c·r·s 원격에서 자식까지 종료. 상세는 위 REPORT.md §5.

- 문서 · 2026-08-02 — **`--decode-buf` 기본값 오기 정정 + 권고 하향(→ 2000).**
  help 와 문서가 기본값을 `2000` 으로 적었으나 실제는 `fec_manager.cpp:33` 의
  `fec_buff_num = 6000` 이다(대입 지점은 CLI 파싱 한 곳뿐). 아울러 링버퍼가
  `conn_info_t` 멤버라 **연결마다** 잡히고 `clear()` 가 전 엔트리를 만져 전부 상주한다는
  점이 어디에도 없었다 — `sizeof(fec_data_t) = 3828 B` → 연결당 `2.2 MB + N × 3.8 KB`.
  루프백 실측(서버 RSS, 클라 1/2/4): 2000 → **+9.4 MB/연결**, 8000 → **+31.7 MB/연결**.
  소크에서 잰 "인스턴스당 36.7 MB" 의 86% 가 이 링(클라 1개분)이었다.
  축출은 **시간이 아니라 도착 개수**(`index++` 라운드로빈, 시간 만료 없음)라 **RTT 는 무관**하고,
  필요 개수 = `(fec-timeout + 경로 지연차 + 지터폭) × 디코더 pps` 다. 단일 스레드 상한
  (15.9 Mbps ≈ 2,150 pps)에서 300 ms 폭이어도 약 650 → **2000 이면 3배 여유**.
  기존 권고 `8000`(과 문서마다 제각각이던 4000/5000/12000)은 근거가 "높은 지터"였는데
  링이 시간 단위가 아니어서 성립하지 않았다. `CLAUDE.md`·`DEPLOY_EXAMPLES.md`·`OPTIONS.md`·
  `README.md` 를 **2000 으로 통일**하고 산정식을 넣었다. 코드는 help 문자열만 변경(버전 유지).

- 문서/테스트 · 2026-08-02 — gw 50 Mbps 부하 계단 + 5시간 소크 측정 보고서
  (`test-results/2026-08-02-50mbps-soak/`) 와 하네스 3종(`mf_ladder3.sh`, `mf_soak.sh`,
  `mf-netem.sh`) 추가. v1.0.8 로 5시간 연속 부하 시 **손실 0.0000535%(1,120만 패킷 중 6개),
  RSS 누수 0, 경로 LOSSY 전이 0건**. gw 50 Mbps 상한에서 실용 한계는 **앱 양방향 각 8 Mbps**
  (증폭 2.67배)로 확인. 2026-08-01 §7-3 이 예측한 **FEC 증폭 톱니**(3.02 → 2.66 → 2.84 → 2.67)
  를 실측했다.
- 문서/테스트 · 2026-08-02 — **계단 하네스(`mf_ladder3.sh`) 결함 3건 수정.** ① 트립 라벨이 창
  '시작' 시점 phase 로 붙어 킬 대상과 어긋나던 문제 ② 완주 판정이 `rc`·실제 전송을 안 보던 문제
  ③ **측정 중 gw 허용치를 1회 초과**(u12M 5초 창 58.07 Mbps)한 데 대한 재발 방지 — 워치독은
  창이 끝난 뒤 판정하므로 첫 초과 창을 구조적으로 못 막는다. 직전 실측 증폭으로 다음 구간
  부하를 **예측해 차단**하고, 게이트(`LIMIT×88%`)에 맞는 최대 레이트로 **스텝 피팅**하며,
  워치독 창을 5초→1초로 줄였다. `mf_soak.sh` 에도 프리플라이트 게이트 추가.
  → 상세 `test-results/2026-08-02-50mbps-soak/REPORT.md` §2-2·§2-3
- 문서/테스트 · 2026-08-02 — **측정 후속 4건.** ① `PRODUCTION_CHECKLIST.md` 의 `-f 20:5`
  권고와 "오버헤드 최대 ~2x" 기술을 실측(6.54x)으로 정정하고 `-f 5:1,20:4`/`--fec-timeout 10`
  권고로 교체, 다중 쌍 표·증폭 톱니 경고 추가 ② `CLAUDE.md` 에 `-f` 다중 쌍 문법·
  `g = 1+floor(pps x timeout)` 식·톱니 표 추가하고 "DEGRADED: RTT 초과" 오기 정정
  (`mud_path_update()` 에 RTT 분기 없음) ③ `mf_sampler3.sh` 에 호스트 UDP 카운터 추가 —
  CPU% 는 포화를 늦게 알리고 실질 경보는 `RcvbufErrors` 증가다 ④ `mf_reorder.py` 신설 —
  단방향 UDP + 자체 시퀀스로 재정렬을 `late` 와 **변위**로 함께 측정. 이걸로 소크 청크4 의
  재정렬 561,216 이 **계수 과대평가**임을 확정했다(실측 변위 평균 2.43 / 최대 6 = 경로
  지연차 5ms 의 정상 인터리브). 이중화(두 경로가 릴레이 호스트·gw 공유)는 테스트망이 3대뿐이라
  **해소 불가 제약으로 확정**해 보고서 §7 에 기록.
- 문서/테스트 · 2026-08-02 — **FEC 강도 A/B**(보고서 §8, `mf_fec_ab.sh`). duplicate 는 종단
  손실이 두 경로 손실의 곱(5%×2%=0.1%)이라 FEC 부담이 작다는 가설을 검증했다. `-f` 는 송신측
  전용이고 FIFO 로 재시작 없이 바뀌므로 c/s 양쪽을 런타임 변경해 600초씩 3구간 측정.
  `10:1,20:2` 로 **회선 −11.3%**(gw 35.18→31.20 Mbps), 손실은 0 → 0.00053%(375,000 중 2개).
  손실 증가는 우연이 아니라 구조적이다 — y=1 에서 그룹 8개 중 2개 손실 확률이 이론값
  0.0008% 로 실측과 맞는다. 기본 권고는 손실 0 인 `5:1,20:4` 를 유지한다.
  **부수 발견**: FIFO 가 보간 테이블을 로그에 찍어 트래픽 없이 후보 검증이 가능하다 — 이걸로
  당초 후보였던 `5:1,20:2` 가 g=7 에서 현재와 동일(y=2)해 절감이 0 임을 사전에 걸러냈다.

- 문서/테스트 · 2026-08-02 — 자원 샘플러에 **시스템 전체 per-CPU 이용률** 수집 추가
  (`test-results/2026-08-02-50mbps-soak/mf_sampler3.sh`). 기존 `mf_sampler2.sh` 는 multi-fec
  **프로세스 하나의** CPU 만 봤다(`/proc/PID/stat`, 100% = 논리 CPU 1개). 그래서 c 의 46.2% 가
  1코어 기준이라는 것은 알아도 **softirq·WireGuard·iperf3 를 포함한 머신 총량은 알 수 없었다**.
  `<out>_cpu.csv` 사이드카에 코어별 `usr/nice/sys/iowait/irq/soft/steal/idle` 을 남긴다.
  기존 CSV 스키마 무변경. mpstat 바이너리 대신 동일 소스인 `/proc/stat` 델타를 쓴다 —
  r 에 sysstat 미설치, ko_KR 로케일에서 소수점이 `,` 로 찍혀 CSV 가 깨짐, `mpstat -P ALL 1 1`
  은 60초 중 1초만 표본. sv1 에서 `mpstat -P ALL` 과 교차검증(오차 ≤0.4%p).

---

## [1.3.1] — 2026-09-10

- 수정 · 2026-09-10 — **클라이언트 pending 큐에 나이 제한(TTL)이 없어 단절 복구 시 수십 초
  묵은 패킷이 배달됐다.** 2026-09-09 SPOF 시험에서 발견했다(→ `CLAUDE.md` §29).
  릴레이 2경로가 27초 동안 함께 사라진 뒤 복구되자 첫 응답 RTT 가 **27,007 ms** 로 나오고
  이후 정확히 프로브 간격(204 ms)씩 감소했다 — 큐에 쌓인 수백 개가 FIFO 로 배출된 것이다.
  터널 안 TCP 는 이미 재전송을 마친 시점이라 그 패킷들은 중복으로 도착해 dup-ACK·재정렬을
  일으키고, 새 패킷은 그 뒤에 줄을 선다. v1.0.2(§19-라)에서 넣은 TTL 이 **서버에만**
  적용돼 있었다(`mf_client.cpp` 의 `pending_pkt_t` 에는 시각 필드 자체가 없었다).
  `pending_pkt_t` 에 `ts` 를 추가하고 `CLIENT_PENDING_TTL_MS 1000` 초과분을 **전송 전에**
  폐기한다 — 실패 경로에 두면 창이 회복된 뒤 백로그가 통째로 통과하므로 의미가 없다.
  **와이어 무변경 · 클라이언트 단독** 수정이라 서버·릴레이를 그대로 두고 한쪽만 교체할 수 있다.
  A/B 실측(같은 스크립트, 바이너리만 교체): 복구 후 첫 응답 RTT **27,007 → 1,978 ms**,
  1초 초과 응답 **60개 이상 → 5~6개**, 클라 stale 폐기 0 → **745개**.
  ⚠️ **남은 2초는 서버 큐다** — `flush_server_pending()` 은 `SERVER_PENDING_TTL_MS` 를
  `mud_send` **실패 시에만** 확인해 성공 경로에는 나이 검사가 없다. 이번 릴리스에서 다루지
  않았다(서버측 별개 변경, 자체 검증 필요). 상세·측정 근거 §29.
- 문서/테스트 · 2026-09-10 — **v1.3.1 서비스 적용 전 검증** (`test-results/2026-09-10-v131/`).
  목표 구조(지사 10 × 각 방향 1 Mbps · 세션별 WG) 30분 소크 — 왕복 **1,875,000 중 유실 7 =
  99.9996%**, **오배송 0**, 가드 초과 2건·**트립 0**. 자원: FD c 70·r 32·s 70 고정,
  RSS 는 램프(c 20.1분·s 9.1분) 종료 후 평탄(c 10.1분·s 21.1분 무변화, r 은 30분 계단 0회),
  CPU 30분 전·후반 c +1.5p·r +0.9p·s +0.4p.
  ⚠️ **30분은 누수 검출 창으로 짧다**(v1.3.0 은 24시간·약 0.005 MB/h). 그래서 같은 하네스로
  돈 v1.3.0 런과 **경과시간을 정렬해 대조**했다 — RSS 가 같은 고원에 앉는다(Δ ≤ 0.7 MB,
  프로세스당 0.07 MB). c 프로세스 CPU 가 09-02 런의 105% 보다 높은 121~125% 로 보이는데,
  v1.3.0 **24시간** 런이 0–30분 **125.3%** 이므로 09-02 가 예외값이다(호스트 busy 는 동일).
  새 코드 경로는 단절→복구 3사이클로 직접 실행시켰다 — 클라 RSS 는 2사이클에서 고원에
  도달해 이후 1 KB 도 안 움직이고 FD 8 고정(v1.3.0 궤적과 동일).
  하네스 `pendttl_test.sh` · 분석 `analyze_leak.py` · `cmp_v130.py` · `analyze_pend.py` 신설.
  → `test-results/2026-09-10-v131/REPORT.md`

## [1.3.0] — 2026-08-06

- 추가 · 2026-08-06 — **동시 피어 32 상한 소진을 진단 가능하게** (`mud_errors.path_full`).
  동시 클라이언트가 `MUD_PATH_MAX`(32)를 넘으면 **33번째부터 전달이 0** 이 되는데 로그도
  카운터도 없었다(실측: 클라 1~32 왕복 10/10, 33~36 은 0/10). 경로 식별자가
  `(local, remote_ip, remote_port)` 라 클라이언트 1개가 슬롯 1개를 쓰고, 넘으면
  `mud_get_path()` NULL → `mud_recv()` 0 → 폐기인데 `mf_server` 는 `log_trace`(레벨 6)로만
  찍어 운영 레벨 4 에서는 흔적이 없었다.
  `mud_errors` 에 `path_full`(주소·시각·횟수)을 추가하고 서버가 30초 sweep 타이머에서
  카운트가 늘었을 때만 `log_warn` 한다(드롭 시점이 아니라 주기 보고 — 폭주해도 로그가 넘치지
  않게). **와이어 무변경, 데이터 경로 동작 무변경 — 진단만 추가**했다.
  ⚠️ **세 상한이 25배 어긋나 있다**: 릴레이 `RELAY_SESSION_MAX` 800 · 서버 `SESSION_MAX` 800 ·
  mud `MUD_PATH_MAX` **32**. 상한 자체는 올리지 않았다 — `sizeof(struct mud_path)=416 B` 라
  메모리는 사소하지만 `mud_get_path()` 와 송신 선택이 전 슬롯 선형 스캔이라 800 이면 스캔이
  25배가 된다. 그 비용 측정이 선행돼야 한다.
  검증 `test_path_slots_unit.c`(32 확보 → 33번째 NULL → 5분 후 전량 회수) ·
  `test_concurrent_clients.py`(1→36 증설로 33번째 절벽 재현) · 경고 출력 실기동 확인.
- 문서 · 2026-08-06 — **`CLAUDE.md` 의 `MUD_PATH_MAX` 8 → 32 정정.** 참조표가 코드와 달랐고,
  그 오기를 믿고 "슬롯 8개가 영구 소모된다" 는 틀린 가설로 조사를 시작했다. 슬롯은
  `mud_path_update()` 가 5분 무수신 PASSIVE 경로를 `memset(path,0,...)` 로 **회수한다** —
  리터럴 `MUD_EMPTY` 대입이 아니라 구조체 zeroing 이라 토큰 grep 으로 놓쳤다. 상세는 §28.

## [1.2.0] — 2026-08-05

- 추가 · 2026-08-05 — **서버 `--accept-local ip`** (반복, 최대 16): 도착한 **로컬 주소**가
  목록에 있는 패킷만 서비스. 서버 인스턴스 하나로 IP 여러 개를 서비스하되 범위를 좁히는
  용도다. mud 는 소켓이 하나라 **주소 2개를 실제로 각각 bind 하는 것은 불가능**하므로
  (`sendmmsg` 가 fd 당 배치 → 전송 큐까지 분리 필요), `-l 0.0.0.0` + 화이트리스트가 방식이다.
  `-l` 이 고정 IP면 커널이 이미 제한하므로 불일치 항목은 **기동 거부**(영구 미일치 = 전
  트래픽 무로그 폐기라 진단 불가), 전부 일치면 중복 경고. 미일치 패킷은 **조용히 폐기** —
  응답하면 서비스하지 않는 주소를 광고하게 된다(§11 과 상충). 상한 초과·`ip:port`·서버 외
  모드는 거부, 호스트에 없는 주소는 경고(VIP 대비). **미지정 시 기존 동작과 동일**,
  와이어 무변경, 서버 단독 수정. 멀티홈에는 policy routing 이 별도로 필요하다.
  상세: `CLAUDE.md` §27.

## [1.1.0] — 2026-08-05

- 추가 · 2026-08-05 — **릴레이 `--upstream-local ip`**: upstream(릴레이→서버) 소켓의 소스 IP
  고정. 릴레이는 `listen_fd`(바인딩됨)와 `upstream_fd`(`connect()` 만) 두 소켓을 쓰므로
  **`-l` 리슨 주소가 upstream 에 상속되지 않는다** — `-l 192.168.100.86:443` 인스턴스도
  upstream 은 커널 preferred source 인 `.85` 에서 나갔다(테스트망 실측). 한 호스트의 릴레이
  인스턴스들을 upstream 구간에서 소스 IP 로 구분해야 할 때 쓴다. 포트는 받지 않고(세션당
  소켓 1개라 고정 포트는 두 번째 세션에서 bind 충돌), bind 실패 시 해당 세션을 버리며,
  릴레이 모드 외에서는 기동을 거부한다. **기본값 미설정 = 기존 거동과 동일**, 와이어 무변경,
  릴레이 단독 수정. 상세: `CLAUDE.md` §26.

## [1.0.9] — 2026-08-02

`--decode-buf` 기본값을 **6000 → 2000** 으로 내렸다. 와이어 포맷 무변경, 수신측 메모리만 바뀐다.
한쪽만 교체해도 되고 상대편 설정과 무관하다.

### 2026-08-02 (`d9f1f6b`, 머지 `57b7f69`)

**계기**: `--decode-buf` 문서 기본값이 `2000` 으로 적혀 있었는데 실제 코드 기본값은 `6000` 이었다
(v1.0.8 시점 문서 정정). 그 과정에서 이 링버퍼가 **연결마다** 잡힌다는 점이 어디에도 문서화돼
있지 않았음이 드러났다.

**왜 문제인가**: `fec_decode_manager_t` 가 `conn_info_t` 멤버(`connection.h:250`)라 클라이언트마다
할당되고, `clear()` 가 전 엔트리를 만져 **전부 상주**한다. `sizeof(fec_data_t) = 3828 B` 이므로

```
연결당 = 2.2 MB + N × 3,828 B
   N=2000 →  9.4 MB/연결        N=6000 → 22 MB/연결        N=8000 → 31.7 MB/연결
```

기본값 6000 에서 `max_conn_num`(200) 을 채우면 **4.6 GB** 다. 다중 클라이언트 서버에서는
CPU 보다 **메모리가 먼저 상한**이 된다.

**왜 2000 으로 충분한가**: 축출이 시간이 아니라 **도착 개수**로만 일어난다
(`index++; if (index == fec_buff_num) index = 0;` — 시간 만료 없음). 따라서 RTT 는 무관하고,
필요한 것은 한 그룹의 도착 시간 폭뿐이다.

```
필요 N = (fec-timeout + 경로 지연차 + 지터폭) × 디코더 도착 pps
```

단일 스레드 상한(15.9 Mbps ≈ 2,150 pps)에서 지연차+지터가 300 ms 여도 **약 650** 이다.
2000 이면 전 구간을 3배 여유로 덮는다.

**실측** (테스트망, 경로 지연차 70 ms 최악 조건, 각 60초):

| `--decode-buf` | 수신 | 손실 | 재정렬 | 변위 최대 |
|---|---|---|---|---|
| 8000 (구 권고) | 37,500 / 37,500 | **0** | 1.77% | 43 |
| **2000** (신 기본값) | 37,500 / 37,500 | **0** | 1.65% | 44 |
| 300 (CLI 최솟값) | 37,500 / 37,500 | **0** | 1.81% | 43 |

**CLI 최솟값에서도 무손실**이다. 6000/8000 은 필요치의 100배 이상이었다.

RSS 실측(루프백, 인자 없이 기동 + 트래픽): **27 MB → 14.1 MB**.

> **upstream 예외**: `fec_manager.cpp` 는 UDPspeeder 유래 코드다. §20-다·§22-가와 같은 사유로
> 예외 적용했다 — ① 기본값이 실사용 규모에서 메모리 상한을 만든다 ② 수정이 상수 한 줄 ③ 상위
> 계층에서 우회하려면 모든 배포가 `--decode-buf` 를 명시해야 한다. 변경 지점에 이유 주석을 남겼다.

**동작 변경 주의**: `--decode-buf` 를 명시하지 않던 구성은 링이 1/3로 줄어든다. 위 산정식 기준
앱 40 Mbps + 경로 지연차 200 ms 급이 아니면 영향이 없지만, 그런 구간이라면 명시할 것.

**함께 변경**: help 텍스트, `CLAUDE.md` / `OPTIONS.md` / `README.md` 기본값 표기.
`DEPLOY_EXAMPLES.md` 등의 권고값은 v1.0.8 시점에 이미 2000 으로 통일했다.

**회귀**: 경로 손실 유닛 10/10, FEC 경계 7/7, RNLC 유닛 11/11. 빌드 경고 0.

## [1.0.8] — 2026-08-02

v1.0.7 이 `rx.loss` 를 살리면서 드러난 후속 결함 1건. 와이어 포맷 무변경, 수신측 단독 수정.

**계기**: 테스트망에 경로별 임피어먼트(path[0] 편도 25ms/5%, path[1] 편도 30ms/2%)를 걸고 v1.0.7
검증 부하를 돌리다 **손실이 더 낮은 path[1] 이 LOSSY 로 배제되는** 역전 현상을 발견했다.

### 2026-08-02 (`c09c0bb`, 머지 `a61ae5b`)

- **수정** — **duplicate 모드에서 느린 경로가 dedup 때문에 거의 전손실로 계산됐다.**
  `mud_recv()` 가 중복 사본을 버릴 때 `path->rx.total` 증가 **전에** 반환해, 사본이 항상 두 번째로
  도착하는 느린 경로는 수신 카운터가 늘지 않았다. `rx.loss` 가 죽은 코드였던 v1.0.6 까지는
  무해했지만, v1.0.7 이 이를 `MUD_LOSSY` 판정에 연결하면서 **그 경로가 통째로 배제**됐다.
  실측(25ms/30ms 두 경로): 30ms 경로가 정상적으로 제 몫을 나르는데도 LOSSY 로 떨어졌다.
  중복 사본도 **그 경로로 실제 도착한 것**이므로 폐기는 전달 계층의 결정이지 경로 손실이 아니다.
  경로 카운팅(`rx.total`/`rx.time`/`rx.bytes`/`idle`)을 dedup 판정 **앞으로** 옮겼다.
  → 상세 CLAUDE.md §25
- **수정** — 같은 원인으로 `path->rx.time` 도 갱신되지 않아, **사본이 전부 dedup 되는 경로는
  `MUD_PATH_DEAD_TIMEOUT`(§21-가)에 걸려 죽은 것으로 판정될 수 있었다.** 위 이동으로 함께 해소.

---

## [1.0.7] — 2026-08-01

경로 손실률 계산이 서로 반대 방향의 두 카운터를 비교하고 있었다. 와이어 포맷 무변경 —
probe 가 싣고 다니는 필드는 그대로이고 수신측의 해석만 바뀐다. 한쪽만 교체해도 된다.

### 2026-08-01 (`fbadeb5`, 머지 `05b317d`)
- **수정** — **`tx.loss` 가 peer 의 송신 수와 peer 의 수신 수를 비교했다.** 손실률은
  "한쪽이 보낸 수 vs 반대쪽이 받은 수"인데 `mud_update_rl()` 이 probe 에서 읽은 두 값을 짝지어
  **같은 쪽의 서로 반대 방향 카운터**를 뺐다. probe 만 오가는 유휴에서는 양방향 패킷 수가
  균형을 이뤄 우연히 실측과 맞았고(그래서 오래 드러나지 않았다), 트래픽이 비대칭이 되는
  순간 방향 불균형을 손실로 보고했다. 실측: netem 10%(누적 실손실 12.9%) 경로가 **93%** 로
  보고돼 `MUD_LOSSY`(임계 78.4%) 로 떨어지고, LOSSY 는 모든 송신 함수에서 제외되므로 비율의
  분모 쪽이 사라져 **그 상태로 래치**됐다. 각 방향을 반대쪽 카운터와 짝지어 계산하도록 고쳤다.
  → 상세 CLAUDE.md §24-가, 회귀 테스트 `test_path_loss_calc.py`
- **수정** — **`path->rx.loss` 가 한 번도 대입되지 않았다.** `mud_path_update()` 의 `MUD_LOSSY`
  판정 조건 두 개 중 하나가 상시 0 인 죽은 코드여서 하향 손실은 판정에 반영되지 않았다.
  같은 함수에서 (peer 송신 수 vs 내 수신 수) 로 계산하도록 추가했다. → CLAUDE.md §24-나
- **변경** — 관측 창에서 손실이 없으면(`rx_acc >= tx_acc`) `tx.loss` 를 **0 으로 갱신**한다.
  기존에는 갱신을 건너뛰어 직전 값이 남았고, 이것이 위 래치가 풀리지 않은 직접 원인이다.
  → CLAUDE.md §24-가
- **문서/테스트** · 2026-08-01 — `multi-fec.md` 빌드 경로를 현행화(`~/multi_udp/multi_fec` →
  `~/multi-fec`, 서버 직접 빌드 예제의 `multi_fec` → `multi-fec`). 사내 위키(Outline)에
  README·사용 가이드·옵션 레퍼런스·배포 예제·운영 체크리스트·CHANGELOG 사본 게시.
- **문서/테스트** · 2026-08-01 — FEC 파라미터 튜닝 측정 보고서
  (`test-results/2026-08-01-fec-tuning/`) 와 재시험 하네스 `mf_ladder2.sh` 추가.

---

## [1.0.6] — 2026-08-01

로그에 남던 PSK 평문 제거. 와이어 포맷 무변경, 로그 출력만 바뀐다.

### 2026-08-01 (`3683ff8`, 머지 `e618138`)
- **수정** — **`--route` 키가 info 레벨 로그에 평문으로 기록됐다.** 기본 로그 레벨이 info 이고
  운영은 systemd 로 돌리므로 journald 에 영구 보관돼, 로그를 읽을 수 있는 누구에게나 PSK 가
  넘어갔다. `obfs_key_fingerprint()`(SipHash 기반, 별도 파생 상수)로 되돌릴 수 없는 짧은 지문
  `kf:330d27b8` 을 찍도록 바꿨다 — 어느 키에 관한 줄인지 구분하고 상대 설정과 대조하는 용도는
  그대로 유지된다. (`-k` 로 준 PSK 자체는 원래 로깅되지 않았음을 확인했다.)
  → 상세 CLAUDE.md §23, 회귀 테스트 `test_relay_routing.py` TC6
- **변경** — `misc.cpp` `process_arg()` 의 명령행 덤프에서 키를 `<redacted>` 로 가리고 debug 줄도
  지문으로 교체. **현재 호출부가 없는 경로**라 예방적 조치다. → CLAUDE.md §23
- **주의(운영)** — 이미 기록된 로그의 평문 키는 바이너리 교체로 사라지지 않는다. 노출 범위가
  문제라면 키 교체나 journal 정리를 별도로 해야 한다.

---

## [1.0.5] — 2026-08-01

수신측 입력 검증 2건. 와이어 포맷 무변경이고 둘 다 수신측 단독 수정이라 상대편을 그대로 두고
한쪽만 교체할 수 있다.

### 2026-08-01 (`7cebcac`, 머지 `9795783`)
- **수정** — **검증되지 않은 `inner_index` / `type` 으로 FEC(RS) 디코더가 abort.**
  FEC 8바이트 헤더를 검증 없이 써서, 그룹에 없는 슬롯이 채워진 것으로 집계되면 조각이 모자란
  상태로 복호에 진입하고 그 실패가 `assert` 안에 있어 프로세스가 죽었다. obfs 토큰은
  페이로드를 인증하지 않으므로 디코더 입력은 사실상 무검증이다. `type` 0/1 제한 +
  `inner_index` 상한 검사 + `rs_decode2()` 실패 시 그룹만 폐기로 바꿨다.
  (`fec_manager.cpp` — upstream 예외, 사유는 CLAUDE.md §22 서두)
  → 상세 CLAUDE.md §22-가, 회귀 테스트 `make test-fec-bounds` 7/7
- **변경** — `Makefile` 에 **`-UNDEBUG`** 추가. `assert` 안에 부작용이 있는 호출이 남아 있어
  `NDEBUG` 가 정의되면 그 호출 자체가 사라져 인코딩·큐 처리가 조용히 누락된다. 패키징 환경이
  `-DNDEBUG` 를 주입해도 무력화되도록 명시 해제했다. → CLAUDE.md §22-가
- **수정** — **릴레이 동시 세션 상한이 없어 FD 고갈.** 세션 하나가 upstream 소켓 + watcher 를
  하나씩 잡는데 idle 만료(60초)만 있었다. 소스 포트를 계속 바꾸면 만료 전에 FD 가 고갈되고,
  투명 중계 모드에서는 HMAC 검증이 없어 누구나 할 수 있다. 서버가 v1.0.3에서 갖춘
  `SESSION_MAX` + LRU 축출을 릴레이에도 적용(`RELAY_SESSION_MAX` = `max_conn_num*4` = 800).
  A/B 실측: 소스 포트 1000개 → 수정 전 세션 1000개(FD 1006), 수정 후 800개(FD 806).
  → 상세 CLAUDE.md §22-나, 회귀 테스트 `test_relay_session_cap.py` 4/4
- **문서/테스트** — `test_fec_decode_bounds.cpp`(+`make test-fec-bounds`),
  `test_relay_session_cap.py` 추가. 기존 스위트 전체 회귀 확인
  (RNLC 유닛 11/11, CLI 90/90, 릴레이 라우팅 7/7, RNLC e2e 9/9, 성능·안정성 26/26,
  다운스트림 다중 24/24, 릴레이 세션 만료 통과).

---

## [1.0.4] — 2026-07-31

멀티패스 경로 장애 거동을 실측하다 발견한 결함 2건 수정. 와이어 포맷 무변경, 수신·상태판정 측만 변경.

**계기**: "국제구간에서 경로 하나가 잘못되면 aggregate에도 문제가 생기지 않나"라는 질문을 격리
netns(veth 2쌍)로 검증하다 드러났다. 운영·테스트망이 전부 `duplicate`라 가려져 있던 결함이다.

### 2026-07-31 (`556edd0`, 머지 `5ee9a71`)
- **수정** — **완전히 끊긴 경로가 배제되지 않아 aggregate가 트래픽 절반을 무기한 유실.**
  `mud_path_update()`의 "프로브 5회 무응답 → DEGRADED" 판정이 MTU 프로브 분기에 가로채여
  `msg.sent`가 1→5→1 로 영구 순환했고, `tx.loss`는 프로브 응답이 있어야 갱신되므로 LOSSY도
  되지 않았다. 계측 빌드로 `st=RUNNING`·`tx.loss=0` 영구 유지를 직접 확인.
  `rx.time` 기반 하드 생존 타임아웃(`MUD_PATH_DEAD_TIMEOUT` 1초)을 MTU 분기 **앞에** 추가.
  실측(5000pps, `-f 20:5`, 40초 스트림 중 10초에 절단): aggregate 유실 **37.73% → 1.38%**,
  duplicate 0.04% → 0.02%(회귀 없음). 복구도 확인 — 경로 복원 후 ~0.5초에 RUNNING 복귀.
  → CLAUDE.md §21-가.
- **변경** — **`MUD_DEDUP_SIZE` 1024 → 16384.** direct-mapped 테이블이라 경로 지연차가 크면
  중복의 짝 슬롯이 덮여 중복이 FEC 디코더까지 통과한다. 실측(지연차 30ms): 1024는 500pps에서
  2.7%, 2000pps 10.9%, 5000pps **24.0%** 통과 → 16384에서 0.2 / 0.7 / 2.3%.
  누출된 중복 자체는 무해하지만(WireGuard가 replay 거부) 수신측 처리가 낭비된다.
  비용 실측: RSS +8KB, **RTT·CPU 변화 없음**(조회가 O(1) 직접사상). 유일한 대가는 합성
  동일페이로드 오탐이 0.26%→0.37%로 늘어나는 것 — 실트래픽은 WG 카운터로 매 패킷이 달라 무관.
  → CLAUDE.md §21-나.
- **문서/테스트** — `test_path_failure.py` 추가(절단·열화·지연차 × duplicate/aggregate = **8/8 통과**).
  격리 netns를 쓰므로 root 필요, 운영 서비스 무영향. 모드 선택 지침을 CLAUDE.md에 추가.

### 함께 측정된 것 (수정 대상 아님)
- **점진적 열화는 aggregate가 오히려 강하다**: 경로B 손실 60%에서 aggregate 유실 0.01%,
  duplicate 0.13%. rate 피드백이 나쁜 경로 몫을 줄이고 FEC가 나머지를 덮는다.
  aggregate가 취약한 건 "느려짐"이 아니라 "갑작스런 단절"이었고, 그게 이번 수정 대상이었다.
- **지연차 30ms에서 aggregate는 상시 재정렬**(414건/깊이150, 유실 0). 경로마다 다른 패킷을
  보내는 모드의 구조적 특성이라 수정 대상이 아니다. duplicate는 재정렬 0.

---

## [1.0.3] — 2026-07-31

두 번째 리뷰(보안/성능/누수) 지적 7건을 검증해 6건을 수정했다. 와이어 포맷 무변경.

**이번 릴리스는 "upstream 미수정" 원칙의 예외를 포함한다.** `packet.cpp` / `connection.{h,cpp}`는
UDPspeeder 유래 코드지만, 지적된 3건이 모두 실제 결함(경계 검사 누락·미초기화)이고 수정이
국소적이라 반영했다. 변경 지점에 이유를 주석으로 남겼다.

### 2026-07-31 (`9bf68d7`, 머지 `0e55e37`)
- **수정** — **릴레이 세션 idle 만료가 60초가 아니라 약 16.7시간.** `RELAY_SESSION_TIMEOUT_MS`는
  이미 ms인데 비교식에서 `* 1000LL`을 한 번 더 곱했다(`get_current_time()`·`last_active` 모두 ms).
  릴레이는 클라이언트 (src_ip, src_port)마다 upstream 소켓 + watcher를 잡으므로 소스 포트가 계속
  바뀌는 환경에서 FD가 고갈된다. A/B 실측: 세션 12개 생성 후 수정 전 **FD 18 고정**, 수정 후 **6으로 회수**.
  → CLAUDE.md §20-가.
- **수정** — **client/server에서 `-k` 미지정 시 소스에 박힌 `default-key`로 동작.** obfs가 켜져 있으면
  `-k`를 필수로 요구하고, 없으면 기동을 거부한다(exit 1). `--disable-obfs`는 면제, 릴레이는 투명
  중계·`--route` 때문에 기존대로 선택. → CLAUDE.md §20-나.
- **수정** — **(upstream) `get_conv()`가 길이 확인 전에 4바이트를 읽음.** 호출지 2곳 모두 FEC 디코드
  출력을 검사 없이 넘긴다. `memcpy` 앞으로 길이 검증을 옮겼다. → CLAUDE.md §20-다.
- **수정** — **(upstream) `conn_manager_t::clear_it` 미초기화.** 생성자에서 `mp.end()`로 초기화.
  전역 객체가 0으로 초기화되어 우연히 동작했을 뿐 표준상 UB였다. → CLAUDE.md §20-다.
- **수정** — **(upstream) `conv_manager_t::last_clear_time`이 생성자에서 동명의 지역 변수로 가려짐.**
  멤버가 초기화되지 않아 conv cleanup 주기가 미초기화 값에 의존했다. → CLAUDE.md §20-다.
- **수정** — `mud_localaddr()`가 포트를 채우지 않아 호출자의 미초기화 값이 `path->conf.local`에
  저장되던 문제(zero 초기화 추가). cmsg 제어 버퍼 정렬 지정으로 `CMSG_*` 접근의 정렬 위반 제거 —
  UBSan이 매 송수신마다 보고하던 항목이다. → CLAUDE.md §20-라.
- **변경** — 서버 세션 만료·축출 시 canonical 매핑뿐 아니라 **그 세션이 쓰던 모든 경로의 peer 태그**를
  해제한다(`session_entry_t.paths`). LRU 축출로 매핑이 사라진 살아있는 세션은 다음 패킷에서 재바인딩해,
  잠깐이라도 "전체 경로 선택"으로 되돌아가지 않게 했다. → CLAUDE.md §20-마.
- **수정** — **`make clean && make -j` 실패.** `git_version.h`를 만드는 실제 파일 타깃이 없어 병렬
  빌드에서 `main.o`가 헤더보다 먼저 시작했다. `git_version.h: FORCE` 타깃으로 바꾸고 기존
  `make git_version` 호출은 별칭으로 유지. CLAUDE.md의 "먼저 make git_version" 우회 절차는 불필요해졌다.
- **문서/테스트** — `make asan` 타깃 추가(ASAN+UBSAN 빌드). 컴파일 경고 전부 정리(미사용 변수 6건) →
  **경고 0**. `test_relay_session_expiry.py` 추가(FD 회수 A/B 검증). `test_perf_stability.py`의
  다수 클라이언트 수신율 문턱을 30%→20%로 조정 — 교차 A/B 측정 결과 코드와 무관하게 24~31%를
  오가는 루프백 한계 항목이라 30%가 상시 경계선이었다(실제 고장은 0%에 근접).

### 리뷰 지적 중 처방을 따르지 않은 것
- **"session/peer 보조 map stale entry"** — 검증 결과 실제 해는 없다. 유휴 경로는 RUNNING이 아니게 되어
  선택 대상에서 빠지고, 주소가 재사용되면 매 패킷 재태깅된다. 다만 테이블 정합성을 위해 위 "변경" 항목을 반영했다.

---

## [1.0.2] — 2026-07-31

추가 리뷰 지적 8건을 코드로 검증해, 그 중 실동작 결함으로 확인된 것과 정합성 문제를 수정했다.
핵심은 **서버가 클라이언트 2개 이상을 동시에 서비스할 때 다운스트림이 목적지를 무시하고 전송되던 문제**로,
리뷰는 이를 "대기열이 세션 정보를 보존하지 않는지 추가 확인 필요"로만 제기했으나 검증 결과 대기열만의
문제가 아니라 서버의 모든 다운스트림 송신 경로에 해당했다.

**와이어 포맷 무변경 · 서버 단독 수정** — 클라이언트는 교체할 필요가 없고, 구버전 클라이언트가 오히려
결함에서 복구된다. 단일 클라이언트 구성(현 운영 형태)에서는 동작이 이전과 동일하다.

### 2026-07-31 (`c8a619b`, 머지 `c7bd955`)
- **수정** — **다중 클라이언트 다운스트림 오배송.** 서버는 소켓 1개에 클라이언트별 PASSIVE 경로를
  갖는데, `mud_send()`/`mud_send_all()`/`mud_send_next()`가 경로를 **전체 경로 표에서** 선택해
  A의 패킷이 B의 경로로 나갔다. B는 conv 불일치로 조용히 폐기하므로 내용 오염이 아니라 **A의
  다운스트림 소실**로 나타난다. 경로에 `peer_id`를 도입하고 `mud_send_*_peer()`로 목적지 세션의
  경로 집합에 한정해 전송하도록 수정(그 집합 안에서 기존 `--multipath-mode` 정책은 그대로 적용).
  실측(루프백, 클라이언트 2개): failover 한쪽 **0% → 98%**, aggregate 각 **33% → 96%**,
  duplicate는 동작하지만 전 클라이언트로 복제되던 N배 증폭이 해소. → CLAUDE.md §19-가.
- **수정** — **`mud_recv()`가 반환한 패킷의 출처를 서버가 잘못 판단.** `mud_io_cb`는 `MSG_PEEK`로
  송신자 주소를 얻지만 `mud_recv()`는 **자체 recvmmsg 배치 큐**에서 패킷을 꺼내므로, 큐에 여러
  클라이언트의 패킷이 있으면 주소와 패킷이 어긋난다. 그 결과 `session_id`의 canonical 주소와 경로
  태깅이 다른 클라이언트에 묶였다(증상: 근거 없는 `alt path` 로그). `mud_get_last_remote()`를
  추가해 실제 출처를 쓰도록 수정 — 위 오배송 수정의 전제 조건이었다. → CLAUDE.md §19-나.
- **수정** — **`mud_delete()` / `mud_create()` 오류경로 메모리 누수.** `sq`·`sq_msgs`·`rq`·`rq_msgs`
  4블록이 해제되지 않았다. `mud->fd`를 생성 직후 `-1`로 두어 부분 초기화 객체도 `mud_delete()`로
  안전하게 정리하고(기존엔 `calloc` 직후 `fd=0`이라 stdin을 닫을 위험), 5곳에 중복돼 있던 free
  캐스케이드를 제거. ASAN A/B 실측: 수정 전 **140,288 B 누수 → 수정 후 0**. 종료 시 1회만
  발생하므로 실사용 영향은 없었다. → CLAUDE.md §19-다.
- **수정** — 서버/클라이언트 pending 큐의 `memcpy` 길이 검증 추가(`NULL`·음수·버퍼 초과 거부).
  현재 호출자는 FEC 인코더 출력뿐이어서 도달 불가 경로이므로 하드닝. 아울러 다운스트림 대기열
  엔트리에 목적지와 enqueue 시각을 실어, 목적지가 사라진 패킷이 큐 머리를 막지 않도록 TTL(1초)
  기반으로 폐기. → CLAUDE.md §19-라.
- **변경** — obfs 인증 토큰 비교를 상수 시간 비교로 교체(`memcmp` → `token_equal`). 허용 슬롯을
  모두 계산한 뒤 결과를 OR 하므로 어느 슬롯이 맞았는지도 수행시간에 남지 않는다. 판정 결과는 동일. → CLAUDE.md §19-마.
- **문서/테스트** — `test_downstream_multi.py` 추가(모드 4종 × 세션 1/2/4 × FEC on/off = **24/24 통과**).
  기존 스위트는 업스트림만 검증해 이 결함을 잡지 못했다. `mud_create()`의 setsockopt 반환값을
  의도적으로 무시하는 이유를 주석으로 명시(IPv4 소켓에서 `IPV6_RECVPKTINFO`는 설계상 항상 실패 —
  실측 `ENOPROTOOPT`. 리뷰가 제안한 대로 실패 시 중단하면 모든 IPv4 기동이 깨진다), `fcntl`
  비차단 설정만 필수로 검증.

### 리뷰 지적 중 심각도가 달랐던 것
- "`mud_delete` 누수 = 높음" — 호출은 프로세스 종료 직전 1회뿐이고 재생성 경로가 없어 **실제 영향 없음**.
  정합성 목적으로 수정.
- "`memcpy` 길이 미검증 = 높음" — 호출자가 FEC 인코더 출력뿐이라 **현재 도달 불가**. 하드닝으로 수정.
- "PRNG 고정시드" — 2026-07-24에 동일 지적을 검증하고 수정하지 않기로 결정된 사안. 이번에도 제외.
- "PRNG 동시 접근" — 스레드를 생성하지 않는 단일 이벤트 루프 구조라 현재 무해.
- "인증 토큰 타이밍" — 사실이나, 인증 실패 시 내장 QUIC Initial을 응답하는 설계라 공격자는 타이밍이
  아니라 응답 유무로 성패를 구분할 수 있다. 실질 위험 증가는 거의 없고 비용도 없어 함께 수정.

---

## [1.0.1] — 2026-07-31

소스 리뷰에서 제기된 결함 8건을 코드로 검증해, 사실로 확인되고 자체 코드에 속하는 3건을 수정했다.
**세 수정 모두 수신측·서버 내부 로직만 바꿔 와이어 포맷은 무변경** — 구버전과 양방향 호환된다
(교차 조합 16종 실측 100% 전달: 신규/수정전/운영배포본 × server/client × failover/duplicate).

### 2026-07-31 (`d3c23fc`, 머지 `dda0f88`)
- **수정** — **mud dedup 오탐으로 정상 패킷이 폐기되던 문제.** 판정 키가 패킷 타임스탬프 단독이었는데,
  `MUD_TIME_MASK`가 bit0을 MSG 플래그로 쓰는 탓에 타임스탬프 해상도가 2µs여서 FEC 그룹 flush 같은
  버스트에서 **서로 다른 패킷이 중복으로 오판**됐다. 복제가 없는 failover/aggregate에서도 발생.
  키를 `(타임스탬프, 페이로드 해시)`로 바꾸고 테이블을 128엔트리 링버퍼 → **1024엔트리 직접사상**으로 교체
  (패킷당 128회 선형탐색 제거).
  실측: 계측 빌드에서 dedup 폐기 **160건 → 0건**, 0.2ms 간격 손실 1.40% → 0.00%. → CLAUDE.md §18-가.
- **수정** — **서버 세션 테이블 무한 증가.** `g_session_to_addr`에 만료·상한이 없어, 유효 토큰 보유자가
  무작위 `session_id`를 주입하면 메모리가 무제한 증가하고 공격 없이도 클라이언트 재시작마다 누적됐다.
  `last_seen` 기반 5분 만료(30초 주기 sweep) + `max_conn_num × 4` 상한과 LRU 축출 추가. → CLAUDE.md §18-나.
- **수정** — **obfs `pkt_type` 필드 손상.** `p[0] = 0x40 | (token[0] & 0x3F) | (pkt_type << 4)`에서
  토큰 비트 4-5와 `pkt_type`이 겹쳐 디코더가 DATA를 PROBE/PAD로 오분류했다. 소비처가 없던 필드라
  와이어 바이트에서 제거하고 디코더는 DATA를 반환(TLS 경로와 동일). 인증에 쓰이는 `token[0]`은
  `p[8]`에 원본 그대로 실려 검증되므로 구버전 영향 없음. → CLAUDE.md §18-다.
- 문서/테스트 — `OPTIONS.md` `--multipath-mode` 표에 누락돼 있던 `aggregate` / `aggregate-duplicate`
  추가, dedup 동작 설명을 수정 내용에 맞춰 갱신, FIFO 미지원·서버측 동일 설정 필요 주의 추가.

### 이번 릴리스에서 다루지 않은 항목
리뷰 지적 중 나머지는 ① 사용자가 수정을 거절한 사안, ② `fec_manager.cpp` / `packet.cpp` 등
**upstream(UDPspeeder) 코드**라 "upstream 미수정" 원칙과 충돌, ③ 기본 설정에서 발현되지 않아
설계 판단이 선행되어야 하는 사안으로 나뉜다. **이 리포는 공개 저장소이므로 미수정 항목의
위치·조건은 여기에 적지 않는다** — 상세는 리포 밖 내부 기록 참조. → CLAUDE.md §18-라.

### 리뷰 지적 중 사실과 달랐던 것
- "dedup 링 크기를 128로 상향" — `MUD_DEDUP_SIZE`는 **이미 128**이었다. 다만 고 pps에서 링이
  5.6ms마다 순환해 의도한 500ms 창이 축소되는 별개 문제가 있었고, 이번 1024 직접사상 교체로 해소.
- "`MUD_TIME_MASK`의 LSB 제거는 결함" — bit0은 `MUD_MSG` data/probe 플래그로 쓰이는 glorytun 설계다.
  따라서 처방은 마스크 수정이 아니라 판정 키 보강이 맞다.
- "판정 키를 `pkt_time + 시퀀스`로 변경" — 시퀀스는 와이어 포맷 변경이라 구버전 호환이 깨진다.
  페이로드 해시로 대체해 수신측만 바꿨다.

- 문서/테스트 · 2026-07-17 (`1cfa235`) — `CHANGELOG` 기록 규칙 예시 정비, `origin/dev`에 push.
- 문서/테스트 · 2026-07-09 (`7af99db`) — `CLAUDE.md`: sv1이 현재 셸이 도는 머신 자체(localhost, `ssh sv1` 거부)임을 명시, `--version` 표기 방식 문서화.
- 문서/테스트 · 2026-07-09 — `CHANGELOG.md` 신설: 버전·날짜별 변경 이력 관리 시작.

---

## [1.0.0] — 2026-07-02

첫 semantic 버전. `MULTI_FEC_VERSION` 매크로 도입 시점의 상태를 `1.0.0`으로 고정.
2026-06-15 최초 커밋부터 이 날까지의 모든 기능·수정을 포함한다(아래 날짜별 상세).

### 2026-07-02 (`40f9eef`)
- 변경 — 소스 내 한글 주석/문자열을 영어로 전환(동작 무변경).
- 추가 — `MULTI_FEC_VERSION "1.0.0"`(`common.h`) 도입, `--version` 표기 정비(버전 + git 리비전 + 빌드시각).
- 변경 — `Makefile` `git_version`이 `git describe --tags --dirty --always`로 실제 git 리비전을 기록(기존 `"local-build"` 하드코딩 제거).

### 2026-06-24 (`0a63fd5`, `96b04f6`)
- 문서/테스트 — 10세션 3시간 아징 드라이버(`run_aging_3h.sh`, mode1 90m + mode2 90m) 추가, `.gitignore` 보강.

### 2026-06-19
- **수정 (`ec9960b`)** — RNLC(mode 2) 코딩 계수를 **랜덤 → Cauchy 행렬(MDS)**로 교체.
  손실 ≤ r이면 임의 k개 수신 시 항상 복구(RS와 동등), 랜덤 계수의 rank 결핍(~0.4% 복구 실패) 제거. 디코더 무변경. → CLAUDE.md §17.
  ⚠️ **처리량 개선 아님**: 재측정 결과 mode2 다운스트림 TCP 2.5~3.0 Mbps로 격차 미해소(가설 검증 실패). 정확성(MDS) 개선일 뿐.
- 문서/테스트 (`66c6c31`, `841c743`, `73dee3f`, `3e15bd5`, `7ae2f72`, `6c800bc`, `5fd9590`, `e8c6325`) —
  다중 세션 스케일/아징 하베스트(`test_scale_sessions.py`) + RT 다운스트림 측정법 추가,
  RNLC mode2 다운스트림 격차 분석(CPU 병목 가설 데이터로 기각 → 지연/재정렬 한계),
  RS(mode1) 운영망 적용 체크리스트(`PRODUCTION_CHECKLIST.md`), 2026-06-18 아징 결과 `test-results/` 이동.

### 2026-06-18
- **수정 (`1941a24`)** — `address_t::get_str()` 회전 정적 버퍼로 교체 → 한 `mylog()`에서 다중 호출 시 주소 표기 뒤섞임 버그 수정. → CLAUDE.md §2.
- 문서/테스트 (`0728d72`, `7958fdf`) — 릴레이 systemd 템플릿/`relay.conf`에 `--auth-interval` 추가 및 불일치 경고, 8h 아징 소크 결과 추가.

### 2026-06-17 (`838cfb4`)
- **추가** — **RNLC(Random Linear Network Coding) FEC 모드 `--mode 2`**. GF(256) systematic 블록 코딩, RS와 동일한 `-f x:y`. → CLAUDE.md §16.

### 2026-06-15 (`311066e` 외)
- **추가** — 최초 릴리스. UDPspeeder V2 FEC + glorytun mud_lite 멀티패스 + GFW 난독화 결합 UDP 프록시.
  - FEC 모드 0(bandwidth-saving)/1(low-latency, RS)
  - 멀티패스: failover / duplicate / **aggregate** / **aggregate-duplicate** (mud_lite)
  - obfs: QUIC/TLS 위장 + SipHash HMAC 인증, QUIC Initial 핸드쉐이크 시뮬레이션, 내장 QUIC Server Initial 응답
  - 릴레이: 단일 upstream / 키별 라우팅(`--route`), decoy 지원
  - session_id 다중 POP 집계, FIFO 런타임 커맨드, TOTP 포트 호핑
  - mud_lite 치명적 수정 다수 포함: timetolerance/keepalive 단위(§1), window 고갈(§4), tx.loss LOSSY 탈출(§5), duplicate 모드(§6)
  - LICENSE + 서드파티 고지, auto-merge 워크플로 정책, timetolerance 30s 문서 동기화
